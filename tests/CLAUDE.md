# tests/

> L2 | 父级: ../CLAUDE.md

这里收口可以离开 Obsidian 真机验证的回归行为。测试直接编译 `src/` 里的 TypeScript 事实源，不维护第二份实现；平台结构检查只钉住「移动端加载前不引入 Node」这条不能由纯函数表达的边界。专业版多出的十条测试按交付物是否存在自动启用，因此同一入口可用于两个版次。

发布通道那条的判据是反的：不是「清单里的东西都在」，而是「仓库根的每个条目都被 publish-v1.sh 分过类」——新增一个第二版专属的顶层目录却忘了写进 PRO_ONLY，后果是把付费交付物推进公开仓库，而 git、rsync 与脚本自己的防泄漏断言**都不会报错**，因为那个断言只认清单里已经写着的名字。

另有一类**跨产物**回归是本仓库独有的：它们不验函数，验的是「代码、模板、契约与首页说的是不是同一件事」。钉住它们的理由是同一个——**这类错都不在写错的那一行报错**。路由那条查每一份存在的施工契约是否都在 `AGENTS.md` 的路由表里：第二版落库时它被漏改，于是整整一个版本里智能体都被它无条件送去第一版契约，只装出一本库而且不报错；目录名那条拿插件常量去 `vault-pro/` 的模板与 `skill-pro/SKILL.md` 的 mkdir 行上核对，不一致的表现只是「待提炼」永远显示空；仓库地址那条查两个版次各自取自己那个 Gitee 仓库——第一版契约会把施工源整份克隆到用户机器，指向 pro 仓库等于把付费交付物发给每个免费用户。三条都只能靠读磁盘上的另一份文件才能发现，这正是它们存在的理由：单元测试再多，也测不出两份文档在说相反的话。

## 成员清单

regression.mjs: Node 内建测试入口，用 esbuild 内存编译事实源；27 项基础回归覆盖版本镜像、Gitee 一键安装入口与作者名片同构、源码与部署库的凭据隔离、本机路径清除、日期严格性、划线身份与批次归并、行尾保真、ISBN、持久化设置验形与灵感格式旧默认换代、灵感集页眉版式与换位幂等、损坏外观配置拒写、书源漏匹配说明、异步命令边界、Apple Books 数据库选择、项目流转事实回滚、移动端 Node 边界，以及 v0.21.0 起的三条状态栏路径纪律——复制口径的默认值必须等于老行为（老库升级时这个字段不存在，默认一改，每位老用户粘出来的双链会突然带上 /Users/他自己的名字，且不报错）、「库内路径 → 本机绝对路径」全 src 只有一处算得出来（两个模块要用它，各抄一份今天字节相同、明天在某一次修 Windows 分隔符时分叉）、状态栏「看的」与「拿的」分离且拿不到本机路径时降级而非罢工，专业版源码存在时再自动追加十条，含出库单往返、「灵感行的格式两侧同源」（同一条灵感有两个写入方，两处各存一份格式串，分叉时不报错），以及「首页的升级口令指向两份契约里真实存在的小节」——那两段口令给用户复制粘贴，里面写死了小节标题，契约改一次标题它就指向一个不存在的地方，而智能体不会报错，只会自己找个看着差不多的地方接着干。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
