# ziminOS - 让零基础学员一键长出能管项目、收灵感、记读书划线、看中国日历、经营人脉、逐级复盘且带完整外观的 Obsidian 个人知识管理系统

TypeScript 7.0 + esbuild 0.28 + Obsidian API 1.13（manifest minAppVersion 1.13.0）+ lunar-typescript 1.8.6 + dom-to-image-more 3.10.2 + jsPDF 4.2.1 + Dataview 0.5.68 + Outliner 4.10.2 + Quiet Outline 0.5.18 + Minimal 9.0.2 + Style Settings 1.0.9 + Pikaicons（图标，MIT，编译进 main.js）

三条红线贯穿全仓库：人主导（一切写入由用户命令或用户建的文件触发，无定时器、无轮询）、脚本驱动（插件内零 AI 调用，同输入同结果）、只用官方公开 API。

**第二条红线的措辞在 v0.16.0 被改写为「写入用户原话的路径上零 AI」。** v0.23.0 又把微信快捷收集向前收了一步：LLM 连「灵感、剪藏还是日记」都不再判断，只判断用户有没有要求保存；要保存就统一调用 `notectl.py inspiration` 写进《兼收并蓄》的 `灵感集.md`。`剪藏/` 是浏览器插件专属落点，微信消息无论长短、是不是链接、是否提到今天都不进入；清洗、链接搬运与插入位置继续交给零依赖确定性脚本。插件本体仍无 AI 调用。

**第二条红线在 v0.13.0 被用户明令放宽：允许联网、允许登录。** 原文是「零网络请求」，代价是学员为了一本书要操作三步——豆瓣插件建档、划线插件导出、再手工把划线复制粘贴进 MOC。用户的判决是：那三步里有两步是机器该干的活儿，把它们留给人不是克制，是失职。放宽最初只对读书笔记生效，用于查书目（豆瓣）与取本人划线（微信读书）；v0.15.0 同样按「机器查得到的就不该问人」将它放宽到中国日历，仅用于在视图打开或切年时核验国务院节假日与调休数据。日历不设定时器、不轮询，24 小时内复用本地缓存，联网失败时继续用上一份可验证数据。v0.22.0 再增加一个**仅本机回环**出口：Eagle 附件桥接通过 `127.0.0.1` 与第一方伴侣通信，不访问互联网，除一次性配对外全部验 256 位随机令牌。其余模块仍然零网络；「同输入同结果」在本机来源（苹果图书 SQLite、Kindle 的 My Clippings）上原样成立，网络来源则有明确降级语义。v0.14.0 沿同一条判据将书的 UID 收口为 ISBN，标签收口为豆瓣分类（`#书籍/{分类}`）。

**v0.36.0 给读书笔记加了第三个方向，而它的形状是被「别把豆瓣惹毛」这一条捏出来的。** 此前两条路都**从书出发**（要读哪本书 → 查豆瓣 → 建档 → 找划线）；用户要的这条反过来——**从划线出发**：在微信读书 / Kindle / 苹果图书里划过线的所有书，一次全进来。方向一反，中间那步就跟着反了：主干命令**必须**查豆瓣（它正是靠豆瓣认出「这是哪一本」），而整架导入一次几十上百本，八十本就是一百六十次豆瓣请求，几十次之后必被拦。被拦的后果不是慢一点，是**一半的书有封面、一半没有，而且谁也说不清断在哪一本**——那比整批失败更糟，因为它看上去像成功了。用户的判决把这件事定死：「一定要避开被豆瓣拉黑。如果没有办法批量，就不要批量，后期手动补录。」于是查豆瓣整件从批量路径摘出去，成了独立命令「补齐书籍信息」：一次只查一本、两次请求，人按一下走一本，慢是故意的；它同时兼任认错之后的纠正入口（查过一次的书再运行必定弹候选，因为这时候他按它的唯一理由就是「上次那本不对」）。这条拆分顺手补上了一个一直没人管的洞——手动建的书本来就没有书目字段，现在与批量导入的书走同一条补齐路。书落在哪儿由用户拍板，理由比原方案更硬：**全部进项目目录、全部「进行中」**，因为每一本都还欠着读完、重排、想过这几件事，那正是「进行中」的定义。三条纪律让它可以反复跑：已在库里的书只补划线绝不重建、**正开着的那一篇一个字都不写**（v0.33.0 立的判据第一次面对几十篇同时写入）、撞名不改名只记账；于是它天然可中断、可续跑，**不需要任何断点状态文件**。

读书笔记经历过一次有意保留的路线修正：v0.12.0 只认三家官方纯文本导出，以复制、粘贴、确认守住当时的零网络红线；v0.13.0 经用户明确授权后把它降为兜底，主路改成豆瓣自动查书目、本机读取 Apple Books 与 Kindle、扫码连接微信读书。两版并存不是两套系统：自动取数失手时，粘贴导入仍复用同一个解析与合并内核，最终都只增不删地写进书的 MOC。

**《赛博永生》的目录名 v0.19.0 起全部是中文，同时提炼多了一个入口。** 那本库的三层结构取自卡帕西的 LLM wiki，落地时照抄了原文的 `10-raw` / `20-wiki` / `90-system`；可这本库里除了目录名之外每一个词都是中文——宪法、README、四种页面类型、账本里的三种记录，全是。目录名因此不是「保留原味」，是**一句没说完的中文里夹着的三个英文词**，而它们恰恰是用户在文件树里天天看见的那三行。改成 `10-原料` / `20-知识` / `90-系统`，数字前缀留着（它保证「原料在上、系统沉底」这个顺序稳定，纯中文名会按拼音随机排），`index.md` / `log.md` 跟着变成 `索引.md` / `账本.md`，账本里的 `ingest` / `query` / `lint` 变成 `消化` / `提问` / `体检`——这三个词 README 本来就是这么教的，只有账本自己还在说英文。**唯独 frontmatter 的键名留英文**（`type` / `kind` / `updated` / `sources`），判据是一条跨两本库的线：**字段名是机器的，字段值是人的**，翻译键名等于让两本库对同一个概念叫两个名字。同版把提炼的入口从一个变成两个：出库单驱动（「提炼一下」，清空全部欠账）之外加一条路径驱动——用户把刚归档的项目 MOC 路径粘过来，就只提炼那一个。加这条不是因为出库单不够用，恰恰是因为它够用得让人不放心：插件在归档那一刻已经记下了这笔账，可用户看不见那张单子在动，而「我刚做完的这件事，现在就想看见它变成知识」是个真需求。两个入口共用同一套搬运与提炼，**入口只决定名单，不决定做法**——两套做法迟早只改一套。

**本仓库自 v0.16.0 起同时交付两个版次，两版共用同一份 `main.js`。** 第一版（免费·单库）是已经在外面流传的那套：一本笔记库，安装契约 `skill/SKILL.md`，README 首页那段一键指令指向它。第二版（付费）是三库系统——《兼收并蓄》收一切还没消化的东西、《以人为本》就是第一版那本工作台、《赛博永生》按卡帕西 LLM wiki 把已完成的项目编译成知识——外加一个口述入口，安装契约 `skill-pro/SKILL.md`。

**第一版另有一条不经智能体的来路（v0.33.0 · 不改插件，规格 §55）。** 契约第二步是 `git clone`，它在开发者电脑上从不失败，于是一直没人把它当成前提——直到学员用 Windows、电脑上没有 Git，智能体第一步就停住。Git 从来不是产品的一部分，用户最终拿到的只是 `vault/` 加四款字体，所以答案不是教学员装 Git，而是把**同一份交付物换一种形态**：`make-v1-package.sh` 把它摆成人看得懂的样子，挂上 Gitee 第一版仓库的发行版（匿名可下），学员照包里的《安装说明》解压、装字体、打开、初始化。两条来路装出来的是同一种库，不是两套做法：清单从契约解析、只从 HEAD 取料、字节与第一版仓库 main 对账。手动升级只做智能体升级的前一半——换程序，不做合并——代价是新片段默认关着，说明里写着去 🎨 打开。

**两份契约的施工源随之改成先取发行版（v0.33.0 · 不改插件，规格 §56）。** 一份 Windows 新电脑上的真机复盘把事情说透了：二十多分钟里真正装库的几步一次没卡，时间全耗在「把施工源拿到手」——智能体自带的 bash 缺工具、git 藏在 `cmd\`、仓库「下载 ZIP」未登录只给 HTML、raw 拼仓库 403/451、`.git` 只读文件删不掉。四条取法里只有发行版附件同时不需要 Git、不需要登录、一次拿全，于是两份契约第二节都按「发行版 → （第二版：用户手里的包）→ git clone」的顺序试，并给出 Python 标准库与 PowerShell 两种写法；第一版的包是给人看的形态，改两次名就是仓库形态，其后步骤一字不改。包名只用 ASCII，因为 Python 的 `urllib` 遇到下载路径里的中文直接抛异常；找包不问 `api/v5` 开放接口（未登录限流，当天就撞上 403），版本号读 raw 上的 manifest、地址直接拼 `releases/download/v版本/包名`。同一份复盘还揪出第二版验收清单自相矛盾的两处（要三份版次标记而只分发两份、要三本库没有 data.json 而 Style Settings 配色本就随库分发）——**清单写错的代价不是一条假红灯，是智能体为了让清单通过而去改装库**。

**全新安装随后从散文变成了代码（规格 §57）。** 取源码修好之后，剩下的慢在智能体本身：几十个步骤、每步一次来回，而一台缺工具的 Windows 上每一步都可能先试错。`installer/` 把全新安装收进一个 Python 标准库写的确定性程序，契约里只留一条一键命令与四种结果怎么处理；Windows 上总是用自己缓存的便携 Python（新系统里的 `python` 只是跳商店的空壳），没装开发者工具的 Mac 下载独立版，两者都钉死 SHA-256。装库在本机不到一秒，自检是逐字节比对，失败就把写进工作区的东西撤掉再交给契约的逐条做法。PowerShell 那一份全文 ASCII——5.1 读无 BOM 的 UTF-8 会把中文读成乱码——而且在 Mac 上无法实跑，第一次必须在真 Windows 上验证。

两版的隔离不是配置开关而是**装配期开关**：第二版安装器写下一个第一版永远没有的版次标记 `edition.json`，`src/core/edition.ts` 在 onload 最前面读它，main 拿到 free 就根本不接第二版那条线——没有命令、没有视图、没有监听，免费库的行为与第二版出现之前逐字节相同。标记可删，删了就退回第一版。失败方向是刻意的：文件缺席、读不动、JSON 坏了、字段不认识，一律回落 free，因为坏标记把付费功能塞进免费库（用户看不见）比坏标记让付费库退化（用户会来问）严重得多。`skill/SKILL.md` 与 `vault/` 的既有内容一个字都不许**因为第二版**而改动——这条护的是第一版用户，不是把那两处冻成化石。v0.17.0 改过 `skill/SKILL.md` 里的克隆地址，理由与第二版无关：GitHub 的 `zhaozimin/ziminOS` 在当时因账号封禁返回 403，那行指令对谁都不成立；v0.35.2 账号解封后又按事实恢复为双镜像。判据因此是**动机**而不是文件名：为第二版让路 = 违规，为「这行字已经不是事实了」= 必须改。

<directory>
docs/ - 设计规格与第三方组件锁定记录；代码、交付物与规格必须同步
eagle-companion/ - 第一方 Eagle 后台伴侣；以官方 item/folder/app API 执行 `项目/容器名` 与 `日记` 归档、附件导入/读取、当前文件夹打开与附件选中，只监听本机回环，与 src/modules/eagle 共同组成一个产品的两个运行时
fonts/ - 四款阅读字体的锁定资产 (4子目录: 文楷 GB 屏幕版＝默认正文、思源宋体 CN、朱雀仿宋、新晰黑＋)；OFL×3 + IPA×1 许可随行，安装时装进用户级字体目录，笔记库内零字节
skill/ - **第一版**交付契约；当前工作区就是用户已命名的笔记库，源码只在外部临时目录施工。不因第二版改动一个字——这条护的是第一版用户，判据是动机而不是文件名
skill-pro/ - **第二版**交付契约 (4子目录: capture 微信保存单一路由、distill 搬运与提炼、scripts 确定性引擎、system-root 两份认路文件；SKILL.md 负责安装/升级)；工作区在这里是系统根而不是笔记库。微信 capture 只有灵感集一个落点，剪藏只归浏览器插件；system-root 与 capture/SKILL.md 必须同步这条路由。
vault/ - 笔记库成品模板；同一交付物内独立放置 ziminOS、Dataview、Outliner、Quiet Outline、Minimal、Style Settings、默认配色与自有 CSS，`.obsidian/.gitignore` 随库保护用户 Cookie、工作区状态与运行缓存；`.obsidian/workspace.json` 是这条规则下唯一被 `git add -f` 破例跟踪的文件——它在学员库里是本机状态，在仓库里是只含 `left-ribbon` 的种子，决定新库第一眼的左侧功能区
vault-pro/ - 第二版特有的笔记库成品 (3子目录: 兼收并蓄 进料口、以人为本 只放一个版次标记叠加件、赛博永生 卡帕西三层结构与它的 Schema)；子目录名就是三本库的目录名，是「那本库叫什么」的事实源。《赛博永生》的三层目录名 v0.19.0 起是中文（`10-原料` / `20-知识` / `90-系统`），与《以人为本》的 `00-inbox`…`90-system` 刻意不同：PARA 那六个是外面世界的既有约定，改名等于让学员查不到资料；这三层是本产品自有的结构，没有外部约定要守。数字前缀两边都留着，它保证文件树的顺序稳定
**开荒不再弹窗（v0.19.0）。** 点完「初始化」原本会跳出一个输入框问用户的名字、替他开出第一个项目。那一步的用意是好的——让他立刻看见一件成品；可它出现的时机恰恰是他刚点完按钮、正等着看结果的一瞬间，于是「装好了」变成了「怎么又要我填」。**开荒的职责到骨架为止**，建项目本来就有一条命令。收尾改成从左右两侧向中间对喷一次礼花（`modules/setup/celebrate`）：同样是告诉他成了，但不索取、不阻塞、不需要他做任何决定。随之作废的是 `templates` 里的首个项目概述与 `VaultSeed` 的 `finish` 钩子——后者唯一的实现者就是那个弹窗，留着是一份没有实现的契约，而**扩展点的价值不在于它可能有人用，在于它现在有人用**。

**导航与 MOC 的 Bases 不再保存项目位置（v0.22.7），存量库由显式命令迁移（v0.22.8）。** 导航用公式把四种 status 翻译成 Emoji，并以 `!file.inFolder("90-system")` 排除整棵系统目录树。项目、领域、书籍与手动 MOC 共用同一块内联 Base：`this.file.folder` 指向宿主 MOC 当下的目录，`this.file.asLink()` 指向它自己，因此「项目文件」、「附件」、「全部」三个视图在整个项目搬进存档后自动重算。状态流转只改位置、status 与 archived，不搜索或替换 MOC 正文。老学员通过「升级存量 MOC 数据库」主动迁移：先预览每个文件的 Base 前后差异，自定义/多 Base 只报告不覆盖；确认后逐篇做乐观并发校验，任何一步失败都逆序回滚本批已触达文件。查询与迁移因此各守一责——模板定义正确终态，迁移器只负责把可证明的旧模板送过去。

**v0.34.1 把书籍 MOC 的文件身份与展示身份分开，并恢复导航原有的封面看板。** `MOC-《书名》` 保留给文件树与容器识别，新书一律把真实书名写入 aliases：手动建书取用户输入去掉最外层《》后的文本，豆瓣建书优先取带副标题的全名。导航书籍视图仍是 `cards`，继续读取 cover，保持 1.35 比例、contain 与 200 卡片尺寸；第一列则由 `file.asLink()` 显示首个 aliases，缺席时回落文件名。新库初始化直接取同一份 `navContent()`，存量系统导航只在显式迁移命令下、且逐字匹配历史模板时升级；既有书籍笔记不根据文件名反猜 aliases。

**v0.35.0 让新建容器在人付出之前检查跨 PARA 同名。** 新建项目、领域与书籍共用 `projects/nameConflict` 检索项目、领域、资源、存档四个根目录的第一层，不递归扫普通子目录。目标路径已存在时直接阻止，其他位置同名则列出类别与完整路径，默认聚焦“取消，换个名称”，只有用户明确选“仍然创建”才继续。名称输入后先查、落盘前再查；未经授权的新冲突停止本次操作。最终容器目录用 `vault.createFolder` 原子创建，不接管一个恰好同名但没有 MOC 的旧文件夹。同版把写入并发、索引快照、导出资源与 Eagle 异步所有权收口成一组可故障注入验证的安全边界。

**v0.35.1 把“换个名称”从命令终点改成同一流程里的纠错。** v0.35.0 的弹窗虽然写着“取消，换个名称”，点下去却只返回 `false`，用户真正换名必须重新运行命令；这是文案承诺了续接、控制流却执行了终止。现在同名弹窗同时展示类别、完整路径和新名称输入框，名称为空、含非法路径字符或再次撞名都留在原窗修正，冲突列表随输入更新；名称可用才继续归属与概述，只有人主动取消才结束命令。落盘前出现的新重名也回到同一纠错流程，不要求重新开始。

**同版修复第一版同步脚本的多字节变量边界。** `publish-v1.sh` 开着 `set -u`，裸写 `$version，` 在部分 Bash/locale 会把中文标点误吞进变量名，表现为两边回归全绿之后才报 `version�: unbound variable`。所有紧邻中文的 Shell 变量改用 `${name}` 明确划界，并由回归扫描整份脚本，防止发布通道再次在最后一步中止。

**v0.35.2 恢复 GitHub，不把它变成第二套发布体系。** Pro 的 GitHub 仓库 `zhaozimin/ziminOS-Pro` 与 Gitee `ziminzhao/ziminos-pro` 承载同一提交；第一版同理是 GitHub `zhaozimin/ziminOS` 与 Gitee `ziminzhao/zimin-os-v1`。README 默认给 GitHub 契约与 clone 地址，同时保留 Gitee 国内镜像；免 Git 安装包仍只从现有 Gitee Release 取，不虚构一个尚未建立的 GitHub Release。第一版发布脚本先对两个目标做非强制 dry-run，再把**同一个提交**依次推到 GitHub / Gitee；任一边分叉就中止而不是 force 覆盖。Pro 本地 `origin` 配置双 push URL，正常一次 `git push origin main` 同步两边——镜像是一个事实的两个入口，不是两套可以独立演进的产品。

**客户 MOC v0.22.11 起进入默认开荒。** 旧设计把客户目录、模板与六张销售/服务表藏在「初始化客户模块」命令后面，结果是客户命令已经可用、主地图却不在库里。新开荒同时收 `contactsSeed` 与 `clientSeed`，直接生成客户目录、客户模板和客户 MOC；MOC 收敛成一张四列名录（人物 / 累计金额 / 交付 / 创建日期），待交付者置顶，其余按金额排序。金额与交付从客户档案里的付费任务实时计算，没付费显示「—」而非伪造 0，创建日期只读档案事实。旧六视图继续注册，已有 MOC 不失效；原初始化客户命令保留为旧库补齐与误删修复入口，所有写入仍是存在即跳过。

**v0.23.0 把“答疑属于谁”与“答疑写在哪”拆开。** 答疑仍住在它所属的领域；客户档案新增只读「客户答疑」视图，以 `obsidian/QA` 标签与 frontmatter 任一属性里的客户双链为双闸。正文普通提及不算归属，链接先经 MetadataCache 解析到真实目标再比较，别名与完整路径都可用。新客户模板天然带块；存量客户必须由用户明确运行一次「补齐客户答疑检索」，升级不静默改正文。

**v0.23.0 的导出只有一棵渲染树。** `modules/export` 用 Obsidian `MarkdownRenderer` 离屏渲染全文，把本地/远端图片内联后由 dom-to-image-more 一次拍成完整 PNG；PDF 只是用 jsPDF 把同一 PNG 放进一张自定义长页面。画布倍率同时受单边与总像素限制，极长文章主动降采样而不裁尾；PDF 单边超出 14,400pt 时等比缩放纸张坐标。页眉、页脚与重复水印在拍摄前成为同一 DOM，PNG/PDF 不维护两套装饰逻辑。

**v0.24.0 给这棵渲染树装上预览，代价是先承认内容与装饰是两种寿命。** 原先 `renderArticle(ctx, file, options)` 一个函数同时负责解析 Markdown、内联远端图、等字体与版面稳定，以及摆页眉页脚水印——前半截昂贵且只该发生一次，后半截廉价却要在每一次拖动滑块时重来。焊在一起，「实时」就只能靠整篇重渲染，而那是卡顿的另一个名字。拆成 `paper`（内容，渲一次）与 `decorate`（装饰，幂等可重放）之后，预览每帧只重放装饰，正文一帧都不重算。**第二条判据决定了预览长什么样：预览不是导出的仿真，预览就是导出物本身**——纸平时停在离屏舞台，预览只是把那个舞台挪进弹窗并按比例缩放，被拍下来的始终是用户刚刚看着的同一个元素。另画一份「示意图」迟早与真身走岔，而那时预览就在骗人，且骗到最后一刻才会被发现。截图前照终值再施一次装饰，堵的是最后一道缝：用户拖完立刻点导出时，排队中的那一帧可能还没轮到。可调的是页眉页脚各自的对齐与留白、水印的排布（平铺防转发 / 单个当落款）、九宫格落点、字号、横纵间距、角度与不透明度；间距加的是**旋转后的外接矩形**而不是文字宽高，否则角度一大，用户明明在把间距调大、看见的却是水印挤成一团。平铺时那张九宫格整行收起而非变灰——无限重复的图案没有「位置」，留一个永远无效的控件比没有更糟。水印**文字**取正文色而不单开取色器：写死的品牌色在暗色主题里是一块看不见的东西。同版加进来的品牌标志则自带颜色——**一条路径，三处各自决定放多大，0 就是那一处不放**；只有一条路径是因为你只有一个 logo，让人在三处各选一遍同一张图不是灵活是重复劳动，而「0 即不放」与「文字留空即关闭」是同一条语法，不必为它再学一套开关。三个尺寸默认全 0，于是老库升级之后导出的那张图与升级前逐像素相同——新功能的默认值应当是「不发生」。「这一段关不关」的判据因此从「文字为空」改成「文字与标志都没有」：只放一枚标志不写字，正是落款最常见的样子。标志的字节由 `logo.ts` 提前解出并降采样后同步递给装饰层，因为装饰层必须全同步——那条链上一旦出现 await，帧与帧就会乱序。整套风格落进 `settings.exportStyle`（core/exportStyle 的十九个字段），因为它的用途是**品牌**，而品牌的定义就是每次都一样；只在点下「导出」之后才存，拖动途中的中间值不算数据，取消就该什么都没发生。


**v0.25.0 把导出的版面交还给 Obsidian 自己。** 上一版的纸是三个常量（760 / 480 / 1600）加一句写死的 `padding: 48px 56px` 拼出来的，遇到宽表格还会按 `scrollWidth` 把纸整张撑开。用户的判决很直接：按编辑区原来的字体大小与宽度，不许自己加宽。判据因此收敛成一句——**发明尺寸就是在替用户排版，而那件事他已经在 Obsidian 的设置里回答过一次了**。宽度、左右留白、字号与行高现在全部量自 `.markdown-preview-sizer` / `.cm-sizer`，也正是「可读行宽」这个设置作用上去的那个元素；探不到时才整套回落，绝不「宽度用量到的、内边距用写死的」——那会拼出一个哪儿都不存在的版面。不再因宽内容加宽是一次**有意的取舍**：超宽表格与长代码行与它们在屏幕上的样子一致，留在自己那个横向滚动的盒子里，而不是把整张图撑宽。

同版还回答了四件事。**颜色**：页眉、页脚、水印各自可挑，空串＝跟随正文色——那个空态必须是默认，因为「跟随主题」在明暗两套配色下各自成立，而任何写死的色号只在其中一套里成立；取色器没有空态，所以每个颜色栏旁边都有一枚「退回去」，少了它用户点过一次就再也回不到跟随主题。**列表参考线**：用绝对定位的伪元素画，不给 `ul` 加左边框——加边框会把整列内容往右推，而对齐编辑区正是这一版的主线；它是全表唯一一个默认为真的新增项，不违反「新功能的默认值应当是不发生」，因为那条护的是「我替用户做了个他没要过的决定」，而这一条恰恰是他点名要的观感。**从电脑选标志**：用户的 logo 本来就在电脑上，逼他先拖进笔记库是把实现细节当成他的工序；选完由插件复制进 `90-system/导出标志`，于是存的仍是库内路径——可同步、换台电脑还认得、手机上也画得出来，同一张图按字节去重不堆副本。**页眉页脚的可点链接**：PNG 是一堆像素，「可点」这个概念在它那里不存在；PDF 的链接注解却与页面内容无关，只是盖在坐标上的一块矩形，所以「整页一张长图」与「可点的页眉」并不冲突。链接只补 `https://`（用户想推广的是 `edu.example.com` 这种写法，逼他先学会带协议是把实现细节当成功课），且只放 http/https 过去——那串字会原样变成 PDF 里的一个动作。PNG 下填了链接会在导出那一刻当场被告知，而不是只写在设置旁边：用户拿到文件发现点不动时，只会怀疑是网址写错了。



**v0.27.0 给纸张两边各配一个开关，判据是「自适应跟的到底是什么」。** 此前纸宽只有一种来路——量用户此刻那个编辑区。它够用，却有一个说不出口的毛病：**跟的是这台电脑此刻那扇窗户有多宽**，是屏幕的偶然，不是笔记的属性；同一篇笔记在笔记本与外接显示器上导出会得到两种宽度，而「品牌每次都一样」正是这个模块的立身之本。所以加的不是一个便利选项，是补一个洞。宽与高分成**两个**开关而不是一个「自定尺寸」总开关：只想把宽度钉成 800 让手机上读着舒服、高度仍随内容长的人是最常见的那一种，合成一个开关他就得连高度一起替自己决定一次。高度只能是**下限**——内容比它高时若按定高裁掉，用户会拿到一张少了半篇的图且毫无提示；而「超出就整体缩放塞进去」会改变字号，与「按编辑区原来的字体大小」那条主线冲突。最小高度落在纸上而不是正文栏上：正文栏撑高会把页脚一起往下推，而「纸至少这么高」要的是纸的下缘。改纸宽看似越过了「内容渲一次、装饰重放无数次」那条边界，其实没有——**Markdown 早已是 DOM，改一下容器宽度浏览器自己会重排**，`resize()` 只写两个 style 属性，一次 MarkdownRenderer 调用都没有，于是滑块照样每帧可拖；但三步的先后不能换（定尺寸 → 施装饰 → 重算缩放），否则水印量到的是上一张纸的高度。同版把每根滑块的读数从只读标签换成**可直接输入的数字框**：拖得到的值受 step 限制（间距一档 4px，永远拖不出 150），而「就要这个数」是真实诉求。换掉的是同一个位置而不是新加一行——三百二十像素那一列里，多一行就少一分预览。


**v0.28.0 两件事，一件是把「量真的」这条纪律再收细一层，一件是把导出从盲盒里拿出来。** 真机量到纸 792px 宽、左边距 88px、右边距 62px——差 26px，肉眼可见地偏左。根因不是算错，是 §46.1 那条执行得**过了头**：编辑区那一栏的左右内边距本来就不对称，右边多出来的那一截是留给**滚动条**的。屏幕上看不出来（滚动条正占着），纸上没有滚动条，于是那份不对称从「看不见的补偿」变成了「看得见的偏心」。判据因此细化成一句：**量的是版面，不是屏幕的补偿件**。左右取平均做单一水平留白——正文栏宽度分毫不变（792−88−62 ＝ 792−75−75），改的只是它摆在哪儿；取平均而不是取大的那个，因为取大的会把正文栏收窄，而那一栏有多宽是用户在「可读行宽」里已经答过的事。第二件：导出那几秒原本只有一条不会动的角落提示，用户的原话是「像盲盒一样」——**一条不会动的提示无法区分「在做」与「卡住」**。改成屏幕正中的进度弹窗，按**阶段**推进而不按字节（dom-to-image 不给回调，字节级百分比只能靠编），每一步自报家门（排版定稿 / 正在栅格化 792×1,360 px / 装进单页 PDF / 写入文件）；条说「还有多远」，标签说「此刻在干什么」，后者才是消除盲盒感的那一半，因为它同时回答了「卡在哪一步」。成功走满停 1.6 秒自退，失败停在原地标红并留着——那句话是用户唯一能拿去问「为什么」的东西。`step()` 等**两**帧才放行：一帧只够把样式算进样式树，第二帧才轮到画上屏幕；不等的话，紧接着那段同步重活会把更新一起压住，条从头到尾只画一次。进度条刻意分成两半——`progressBody` 纯 DOM、连 `createDiv`/`setText` 都不用，`progress` 只当 Obsidian 宿主——好让演示页搬走同一份代码；这条第一版没守住（演示页当场抛 `label.setText is not a function`），教训是**文件头写着「零依赖」就不能偷偷依赖**，那不是风格问题，是那句话真不真的问题。


**v0.29.0 两件事：让链接看得见，让明暗能自己定。** 用户报「输入链接后 PDF 里没正常显示」，用真源跑了五个用例才看清——机制没坏，坏的是一个特例：`buildLine` 那句 `if (!input.text && !showLogo) return null` 里**链接根本没参与判断**，于是「只填链接不填文字」得到的是整行不存在、没有可点区域、而且不报错。又一个填了却什么都不发生的输入框。修法不是加错误提示，是**让链接自己成为这一行的内容**——他要推广 `edu.example.com`，那就把它印出来；印的是他填的原文而不是补过协议的 href，协议只在写进 PDF 注解时补。第二层问题更要命：**看不见的可点区域等于不存在**，读者根本不知道能点。所以有链接就给那段字加下划线——**但只在 PDF 下加**，判据是那条线说的话真不真：它的含义是「这里可以点」，而在 PNG 里那句话是假的，画了就是在纸上撒谎；于是切换格式时下划线跟着出现或消失，比导出后那句提示更早更直观。第二件：新增 `theme`（跟随/亮色/暗色），默认跟随——换了默认，所有人下一次导出的底色都变了；但常年用暗色写作、却要交一张白底给客户是很常见的一件事，而为了导一张图去把整个 Obsidian 切亮再切回来，是让人替机器干活。做法是往**舞台**上挂 `theme-light`/`theme-dark` 一个类，可行性先验过再动手（主题把配色变量定义在不带 `body` 限定的类选择器下：Obsidian 自己如此，实测 Minimal 全篇 347 处里只有 1 处带 body），于是同一套变量在这棵子树里重算一遍，纸换了底色而屏幕上其余部分一动不动。三条边界：挂舞台不挂纸（被拍的是纸，它自身的计算样式里只剩解算好的颜色——与「缩放只加在舞台上」「透明度写在舞台上」是同一条理由的第三次使用）；绝不去动 `document.body` 的主题类（那会把整个界面闪一下）；明暗必须排在装饰之前（装饰层要读正文色去定水印颜色），因此那条链是**明暗 → 尺寸 → 装饰 → 量**。**中间那条边界在 v0.31.0 被推翻了**，理由见下。


**v0.30.0 把导出面板压到六行，并让日历分出两种事实。** 面板改版之前固定 34 行、约 2,400px 高；压缩的两个来源都不是「把控件做小」——其一，**说明不常驻**：每行下面那一到两行灰字才是吃掉高度的大头（一行 70px → 30px 出头），它们搬进了名字的悬停提示；其二，**关着的段落不画**：页眉页脚水印各得一个开关，关掉时下面那一堆根本不存在。实测三段全关约 265px、全开约 820px。版式从「说明在名字下」的三件套改回**左右两列**，名字一条 68px 定宽列，因此滑块表里的 name 一并收成两个字，完整说法进 desc。三个开关把「关闭」从**推断**变成**声明**——此前想临时不要页眉，得先把自己写好的那行字删掉；而它们的升级语义才是最要紧的部分：老库的 data.json 里没有这三个键，取 false 会让所有人的页眉页脚水印一夜消失，取 true 会让空段落平白展开，唯一对的默认是**从内容推导**（那行字还在，说明它本来就在显示）。纸张两个开关合成一个，理由很具体：高度本来就是下限，拖到最小就等于没约束，于是「只想钉宽度」在一个开关下照样做得到；同版加 A4/A3 预设，**只在 PDF 下露面**——那是印刷开本，图片没有「开本」这回事。`listGuides` 退场：参考线恒开，留一个永远为真的字段只会让人以为它还能关。控件列分家成 `panel.ts`（modal 753 → 329 + 452），判据与 settings/settingsPanels 同源；它**不 import obsidian**，于是演示页把**同一列控件**搬进了浏览器——演示页从「照着画一份」变成「用的就是那一份」，driver 也从 576 行降到 347。

**同版日历长出两种颜色，而它们不是三选一。** 这一天（或这一周）的复盘写过了涂绿，今天涂红；**今天也可能已经写完，那恰恰是最该一眼看见的一格**，做成三态就只剩一半信息。于是两条独立的视觉通道：写过了是内容的事实（淡绿底＋绿色日期），今天是此刻的事实（实心红块），两者兼有时红块之上补一枚绿点。数据走注入，与 `CalendarPeriodOpener` 同一条路数——`CalendarNoteProbe` 只回答「这一格要不要涂绿」，目录规则与文件名格式全归 review，让日历自己学会一套就会有第二处对「日记住哪儿」的理解。重画有两条路且缺一不可：点一格之后立刻重画（点一格更常见的结果是「那篇已经在了，只是打开它」，那时没有任何 vault 事件），以及听 create/delete/rename 防抖 80ms（复盘也会在日历之外被建出来或删掉）。

**v0.36.1 修的那一个，藏在「插件重载」这四个字里。** 用户报右侧功能区不知不觉出现了两个「中国日历」。日历没有任何「再开一个」的入口——三条路走的都是同一个 `ensureSideLeaf`，而它的官方语义正是「有就用那个」。所以第二个标签页不是谁点出来的，是那句「有就用那个」在某个时刻答错了。答错的时刻精确到一拍：`Plugin.registerView` 的卸载钩子**只在用户主动停用时**才 detach 叶子，插件更新走的 `disablePlugin` + `enablePlugin` 不算，宿主刻意把老叶子留着、等重新注册时自己还原；而它还原的做法是先**同步**把叶子换成空视图、再在微任务里装回来——那段窗口里 `getLeavesOfType` 返回空数组，叶子还在，只是此刻自报「我是 empty」；偏偏 `onLayoutReady` 在布局已就绪时是同步回调，而 `registerView` 与它之间一次 await 都没有。三段宿主语义严丝合缝地对上，于是每重载一次多一个。修法不是延时重试、不是数微任务、也不是事后清扫多余叶子——那三种都是**对一个正在变的答案反复采样**；把提问挪到 `registerView` 之前就够了，那一刻叶子要么还带着宿主的墓碑视图（它如实报出原类型），要么根本还没被反序列化出来，两种情形下答案都是真的。存量重复不自动收掉：那是一次没被用户请求的界面改动，而根因去掉之后它不会再长。同模块的「最近文件」从来没犯过这个错，差别只在它压根不自动展开——**同一个宿主语义，谁在那一拍里说话决定了谁会踩坑**。

**v0.32.0 修的两处，都藏在一份已经交到用户手里的 PDF 里。** 用户报「PDF 依旧跳不到我设的网站」，而拆开那份 PDF 一看，链接注解**是在的**——`/URI (https://edu.zhaozimin.cn/)` 一字不差。坏的是它落在哪儿：页面高 1023pt，正文那几条链接的 y 都在 0–1023 之内，页脚那条是 `Rect = 292.5 -888 349.5 -919.5`，**y 是负的**，整块可点区域被放到了纸面之外。根因是同一件事有两套算法：正文走客户端矩形，页眉页脚走 `offsetTop` 逐级累加。而 `offsetTop` 量的是「离最近那个**定位祖先**多远」——页眉页脚那一行只设了 `display:flex`、没有 `position`，于是它子元素的 offsetTop 量的是离正文栏顶端多远，再加上行自己的 y，同一段距离就算了两遍。页眉在 y≈0，算两遍还是 0，所以它一直是好的、也就一直没人发现；页脚在 y≈1240，一加就是 2480，落到纸外面。修法不是给那一行补一个 `position: relative`——那是让一处几何依赖一条为**别的目的**写下的 CSS，下一个人删掉那行样式时不会知道自己弄坏了什么；修法是把两套合成一套，让「页眉的链接能点」与「正文的链接能点」从此是同一个函数的两次调用。

同一份 PDF 里还躺着四条指向不存在域名的链接：`https://moc数据库代码/`、`https://项目检索数据库/`……全是用户笔记里的库内双链。`exportLinkUrl` 那条「没写协议就补 `https://`」的规则，对**设置里那个输入框**是对的（他写 `edu.zhaozimin.cn`，意思就是省略了协议），对**DOM 里的 href** 正好相反——一个没有协议的 href，意思是「这根本不是一条外链」。同一句话在两种输入里意思相反，就不能交给同一个函数。这不是少判了一个条件，是一条规则被用到了它不成立的地方；那份文件的注释里甚至早就写着「只放 http/https 过去」，只是实现没兑现它，因为补完协议之后它们**确实**是合法的 https 网址。

**v0.31.0 三处，全部只有把插件装进真机、打开开发者控制台才看得见。**

其一，日历整个不见了。上一版给它新加了一个 `private async open(...)`，而 `View.prototype` **自己有一个 `open`**——Obsidian 打开视图时调的正是它。我们的同名方法把宿主那份盖掉，于是宿主调进来、参数全是 undefined，在 `period.label` 上抛异常，右侧栏一片空白。它编译期一个字都不报，因为 `obsidian.d.ts`（8482 行）里根本没写这个成员：**声明文件是宿主答应支持的那一部分，不是它运行时真有的那一部分**。真机控制台沿原型链枚举才看得见 `View.prototype` 有 `open`/`close`、`Component.prototype` 有 `load`/`unload`。改名叫 `openAndRepaint`，并加一条回归——两个 ItemView 子类的成员名逐个比对宿主那份名单，撞上就当场变红。这条教训比这个 bug 大：**继承宿主的类时，方法名只能取那些不可能是框架词汇的**，`open` 是个通用动词，通用动词早就被人用了。

其二，导出的纸右边总空一条。它有两层，都无声。表层是纸自己带着一份主题给的内边距（`.markdown-preview-view`，真机 32px），而正文栏拿的是**整张纸的宽度**——于是正文栏从这份内边距里整整溢出 32px，`measure()` 又按含溢出的 `scrollWidth` 报尺寸，拍下来的画布比纸宽 32px，多出那块没有纸的底色。页边因此收口成一处：纸的内边距清零，正文栏改成跟着纸走，量纸宽问纸自己的宽。里层更值得记：`measureSource` 挑的是第一个**存在**的元素，而一篇笔记只要被阅读视图渲过一次，那个 `.markdown-preview-sizer` 就一直留在 DOM 里，切回编辑态它不消失、只是宽度变 0——于是编辑态永远先撞上它、整套回落 FALLBACK，**v0.25.0 那条「量自编辑区」的主线一次都没有真正跑过**，而纸看上去始终有模有样。改成挑第一个**量得到**的。「存在」与「量得到」在 DOM 里从来不是一回事。

其三，切明暗切不干净。v0.29.0 只往舞台挂一个类，赌的是「主题把配色变量定义在不带 body 限定的选择器下」。这个赌注对**变量**基本成立，对**规则**不成立：凡是写成 `body.theme-dark .foo { … }` 的（Minimal 有、Style Settings 生成的有、这本库十三个片段也有），子树里挂多少个类都够不着那个 `body`——纸换了底色，Bases 表头、代码块、引用块却留着原来那身颜色。用户的判据是「把整个 Obsidian 想象成换了明暗主题，然后再导出」；那就别想象，**直接换**：类挂到 Obsidian 自己挂的那个地方，每一条规则、每一个片段都按新主题重算一遍，不存在够不着的死角。代价是界面会跟着变一下——而那恰恰是它的意思，预览就是导出物本身，屏幕上这套配色也就该是导出物那套。换回来必须无条件，所以 `release()` 的第一件事就是还债，导出失败、按取消、Esc 关窗三条路最后都汇进同一个 finally；记的是「原来有没有这个类」而不是「原来是哪一套」，因为两个类都不在（跟随系统配色）时，后者会凭空加出一个。

**v0.26.0 修的五处，全部只在真机导出的那张图上看得见。** 它们值得单独记一笔，不是因为难，是因为**几何对、类型对、断言全绿，错却都在图上**——这类错只有把成品拿在手里才会现形。其一，参考线画给了每一层，于是列表左边多出一条贴边的竖线；顶层没有父级，那条线什么都不表示，选择器收到 `li > ul` 才对——**有父级才有「归属」可言**。其二，编辑态量错了元素：`.cm-sizer` 是整个编辑器那一栏、左右内边距为 0，量它会得到「纸和编辑器一样宽、正文顶到纸边」；正文栏在编辑态是 `.cm-content`，它的内边距就是 `--file-margins`。顺带给留白加了下限 48px——这不是「不发明尺寸」的回潮，它回答的是另一个问题：**纸总得有边**。其三，纸原本停在 `left: -100000px`，而 Bases 这类视图按「进没进视口」决定要不要画单元格，于是导出的图里表格有边框、有行、连「4 个结果」都对，单元格却是空的；停靠位置挪回屏幕左上角、靠透明度隐藏，透明度写在舞台上而不是纸上（dom-to-image 读的是被拍元素**自己**的计算样式，祖先不参与）——与「缩放只加在舞台上」是同一条理由的第二次使用。其四，只有页眉页脚上了链接注解，用户笔记里本来就有的外链导出后全变成像素；那是**不该由用户承担的退化**，`anchorRegions` 因此逐个收集 `a[href]`，用 `getClientRects()` 逐行取而不是一个整包围盒（横跨两行的链接，包围盒会把中间那段无关的空白也圈成可点）。其五，顺序反了：旧流程点完「导出」先关窗、再栅格化好几秒、保存框最后才来，而那几秒完全可能白花——用户在保存框里按取消。新顺序是**先问去处、再做图**：保存框立刻弹出，取消就把预览留在原地（取消保存与导出失败对用户是同一件事：这次没导出成，而他刚调了十分钟的那套风格不该因此消失）。

**v0.23.0 把两张内建总控台纠正为统一的 MOC 前缀。** 新初始化固定生成 `02-areas/人脉/MOC-人脉.md` 与 `02-areas/客户/MOC-客户.md`，人物默认归属与两张总控台之间的双链都从同一组路径常量推导。两张文件虽然长得像普通 `MOC-目录名`，却不是项目/领域卡片容器；存量 Bases 迁移必须按内建名单排除，不能给它们误补项目数据库。口述名单扫描同时排除新名与旧名，升级期两代文件并存也不会把 MOC 当成人。

**一篇复盘笔记不再因为诞生方式不同而变成两种东西（v0.23.1）。** 套模板原本焊在 `openPeriodNote` 那条链上，而那条链只有五条打开命令与日历点击够得着；学员在 `05-diary/01-daily` 里右键新建一篇改成今天日期，或者点开导航行里那条还没有目标的双链，拿到的都是一张白纸。这不是模板坏了，是**判据挂错了地方**——同一篇日记，谁把它建出来决定了它长什么样。projects 模块早就答过同一道题（`registerCardAutoInit` 把出生登记从命令里摘出来挂到 vault 事件上），复盘缺的正是那一半。现在它挂回「一篇复盘笔记诞生」这件事本身：**入口不决定结果，名字与位置才决定身份。** 同时修掉的是一个没人点名的漏洞——导航行里的上级双链只写标题不写路径，Obsidian 按「新笔记位置＝当前目录」落盘，于是点一下「本周」，周记被建进了日记目录；`periodFolderOf` 既然是「哪一级住哪儿」的唯一事实源，认领时就按它归位。不改模板去写全路径，是因为存量笔记的裸标题救不回来、认领这一半无论如何都得做，再加一套就是同一个结果的第二套机制。五道闸守住边界：非 Markdown 不碰、插件自己刚写过不碰、**有内容不碰**（零字节才算刚诞生）、复盘目录之外不碰、名字严格解析不出周期不碰。它不提问也不新增开关——建一篇笔记不是一次复盘，点开明天的日记更不是。

vault/.obsidian/plugins/ziminos/ - 插件安装位；package.json 的 version 才是版本唯一事实源，Obsidian/Eagle 两份 manifest 都由构建链同步，main.js 是刻意入库的构建产物（四十枚命令图标、三枚设置页专用图标与六个品牌 logo 的 SVG 也在里面），styles.css 服务二十四个笔记内视图、中国日历与最近文件两个 ItemView、Eagle 附件呈现、外观开关浮层、文件夹计数、状态栏当前路径、导出预览弹窗（连同会被拍进 PNG/PDF 的页眉页脚）、八张设置页与作者名片（手工维护，不经 esbuild），ziminOS-Eagle-Bridge.eagleplugin 是同版伴侣的可安装 ZIP 产物；三份状态文件（holiday-cache / recent-files / cursor-positions）由插件在运行时自建，升级一律不碰
vault/.obsidian/snippets/ - 十三个 CSS 片段，外观包的可拆装部分；十一个默认启用，全部由右下角外观开关逐个开关。第十三个「突出废弃内容」把 Markdown 删除线收成系统语义：编辑/阅读两态同时弱化文字、铺淡警示色背景、画 2px 警示色线；段内双链另加风险底色、边界与下划线，已断双链升为红色双边界，不插标签打断原句。appearance.json 的 enabledCssSnippets 是它们开着还是关着的唯一事实源。片段吃的是宿主的 DOM，宿主改结构它就会无声失效：v0.35.3 的「图片居中显示」即是——Obsidian 1.14 把实时预览的图片从 `.cm-content` 的块级 widget 改成住在 `.cm-line` 里的行内原子区间（方向键可进出、空格看大图），行内盒子吃不到 auto 外边距，于是居中的对象从图片本身变成收着它的那一行
installer/ - 全新安装的可执行事实源（v0.33.0 · 不改插件，规格 §57）：Python 标准库写的核心 ziminos_install.py 负责判工作区、铺库、装字体、逐字节自检与失败回滚，install.ps1（纯 ASCII，Windows PowerShell 5.1）与 install.sh（macOS/Linux）只负责准备 Python 与从发行版下载包；两份契约的全新安装先交给它，智能体只剩「运行一条命令、读 .ziminos-install-result.json、告诉用户」。升级暂时仍走契约里的升级分支
tests/ - 两版共用的审计与回归入口；直接编译 src 事实源，并以专项测试钉住微信单一路由、客户答疑双闸、导出画布/PDF 尺寸、手动安装包与说明同构、版本同构、后台写入的时机与值（writes.mjs：按真机顺序喂事件，验写没写、写到哪一篇、写了什么值）、侧栏叶子的唯一性（calendar-leaf.mjs：按宿主重载语义喂事件，验多没多出一个标签页）与既有业务行为
src/ - 插件源码 (2子目录: core 无业务基础设施与命令/视图/版次闸门；modules 含 setup、projects、books、inspiration、calendar、review、contacts、appearance、format、editing、eagle、export 当前笔记导出、explorer、legacy、ribbon、eternal、about)
</directory>

<commands>
四十二条命令，身份（id / 中文名 / 图标 / 分组）全在 src/core/commands.ts，一律经 CommandRegistry 注册。默认七条摆进左侧边栏，其余三十五条勾一下就上；「补齐客户答疑检索」是存量档案的一次性迁移，「导出当前笔记」是低频交付动作，三条整架导入是一辈子按几次的搬家动作，都不预占默认边栏。图标全程走 addIcon / addRibbonIcon / Command.icon，十一分组颜色不变；四十三枚图形里有一枚由三条整架导入命令共用——它们做的是同一件事，只是来源不同，而那也正是它们共用同一个内核的原因。

**v0.37.0 把新库第一眼的那一列收到八个按钮，而它由两半拼成。** 用户在自己的《以人为本》里逐个拨了十几下，拨出来的结果是：关系图谱 + ziminOS 那七条（新建项目、记录灵感、打开今天的日记、写复盘主题、新建人脉、记人情、打开外观开关），Obsidian 自带的快速切换、白板、日记、模板、命令面板、数据库六个收起。把这个结果变成所有人的默认，要动的却是两个互不相干的东西：**ziminOS 自己的按钮归 `DEFAULT_RIBBON_COMMANDS`，Obsidian 自带的归它自己 workspace.json 里那把 `item.hidden` 的锁**。前者去掉旧版三条（切换笔记库 / 帮助 / 设置）——这是对 v0.17.0 的纠正：当时那句「一个需要先去设置页勾选才回来的按钮等于没有回来」把「功能要够得着」与「它默认就该占着一格」说成了一件事，而前者由命令注册与设置页那张清单保证得好好的。后者做成随库交付的**种子**：`vault/.obsidian/workspace.json` 只含一个 `left-ribbon` 键，安装时随 `vault/` 整份铺过去。不让插件去写它，是因为那条路只有两种走法——赶在 Obsidian 把内存里那份写回磁盘之前（赌时序），或者伸手去碰非公开的 `leftRibbon`（开红线的口子），而种子在安装那一刻落地，两样都不需要。

种子这件事有三处刻意为之。**一是它被 `git add -f` 破例跟踪**：`vault/.obsidian/.gitignore` 里 `workspace*.json` 那条规则在两端的意思正好相反且两端都对——学员库里它是每分钟都在变的本机状态，仓库里它是一份只读一次的种子；`publish-v1.sh` 因此必须显式 force-add，否则目标仓库 `git add -A` 会把它漏掉，随后那句 `git status --porcelain` 判空还会把「只改了种子」的那一次报成「无需发布」。**二是键的先后同时决定图标的先后**——`WorkspaceRibbon.load()` 读完隐藏状态之后正是按 hiddenItems 的键顺序给 items 排序，所以种子照抄的是用户真机上那份键序，而不只是那几个布尔值。**三是它天生脆弱且脆弱得温和**：键是 `插件id:界面文案`，随 Obsidian 的界面语言与版本文案变化，对不上时后果只是「多出几个图标」，不是报错——这是 Obsidian 唯一的正门，学员也能从「设置 → 外观 → 功能区」逐个拨回来。边界：**只对全新安装生效**。已经装好的库里 workspace.json 是学员自己的状态，升级分支一个字都不碰它；当年摆出过旧版三条的人，存档覆盖默认值，按钮不会被撤走。
</commands>

<settings>
设置页按系统模块切成八张标签页（开荒 / 项目 / 灵感 / 复盘 / 人脉 / 编辑 / 文件 / 边栏），页标识与 modules/ 下的目录同名，一页只回答一个系统的配置问题；顺序即 main.ts 的装配顺序，边栏排在最末是因为它必须最后装配（照花名册摆图标，摆时花名册必须收齐）；目录名等设置也各自归还给它服务的那个模块，而不再堆在页尾一个统称「高级」的折叠区里——每页自己有一个，永远在页尾。多个模块不各占一页，判据是用户会到哪里找它：Eagle 附件住「编辑」页；外观开关与作者名片住「开荒」页（v0.9.2 拍板，开关排在初始化之后——外观包是开荒交付物的一部分；名片是页尾落款、排在高级折叠之后，画的与 README 顶部是同一张）；读书笔记的三项（标签前缀、标签条数、微信读书的连接）住「项目」页，理由不是它少，是**一本书就是一个项目**——书落在项目目录、走项目的四态流转归档，给它单开一页等于在界面上否认代码里那条已经成立的事实。反过来，v0.16.0 的 explorer 照同一条判据拿到了自己的一页「文件」：它的三项——开不开、数什么、含不含子文件夹——彼此独立，缺一个就说不完整，而它管的是屏幕上那棵目录树、不属于任何一套笔记；「一个控件撑一整页」说的是外观开关那一个开关，不是这三个。它排在「边栏」之后不是凑数——命令坞与文件浏览器同住屏幕最左边那一条，学员找「左边那个数字的设置在哪」时翻的正是它旁边那一页。排版同样不再单列（v0.17.0 并进「编辑」页，用户明令：「排版和编辑应该算作一起，它们属于一类」）：这一条的判据比其余几处并页都直白——**它们发生在同一个时刻**。你在编辑器里敲字，粘贴变成链接、附件交给 Eagle、光标记住位置、走开之后这一篇被整理成标准写法，是同一件事的三个瞬间；排版若单列成页，学员得先分清「整理格式算不算编辑」才知道该翻哪一页，而那个问题本身就不该存在。页内的先后是「立刻发生的」在前、「走开之后发生的」在后：粘贴、Eagle 附件与光标在上，「排版」一道小标题带着自动开关与九条规则在下。客户也不再单列（v0.14.0 并进「人脉」页）：它单列的旧理由（客户与人脉是两个物种）对命令仍成立、对设置不成立——两页加起来只有四个字段，其中两个还是同一种东西；分页的意义是「一页看完就不必再往下翻」，而不是「每个物种都得有张页」，页内切段不切页。导出刻意不占第九张页：它那十五个字段全部只有配着预览才说得清（「间距 140px」是疏是密，离开那张纸没有答案），而设置页天生看不见那张纸——于是导出弹窗自己就是它的设置页，改完即是所见，点下导出即落盘。八张页的身份（短名 / 图标 / 模块全名 / 交付状态）收在 src/settingsModel.ts 的 TABS 一张表里，标签栏与每页页头都从它出；图标取自 COMMAND_ICONS，与左侧边栏、命令面板同一套 Pikaicons 笔画图形（v0.9.1 起弃 emoji），标签栏是「整行底线 + 当前页填充胶囊」的样式，标签自然宽度左对齐、一行排下不换行；模块清单不再单列一份，标签栏本身就是那份清单。加一页 = TABS 加一行 + src/settingsPanels.ts 的 render 表加一个渲染函数，后者的 Record<TabId, …> 会在漏写时报编译错。界面自 v0.17.0 起是三个文件：settingsModel 回答「有什么」与「要别人替它做什么」，settings 回答「骨架怎么画」，settingsPanels 回答「每一页塞什么」——两次分家的判据都是变更理由不同，触发点都是那条 ≤800 行。
</settings>

<views>
V2 起的二十四个笔记内视图由插件自渲染：笔记里只留一行 ```ziminos 代码块 + 视图名，逻辑住在 main.js、样式住在 styles.css。v0.23.0 新增「客户答疑」，以标签 + frontmatter 客户双链双闸检索领域答疑；「客户名录」与旧六张客户总控视图继续注册。中国日历仍是独立 ItemView；「关于作者」仍是唯一不读 VaultIndex 的笔记内视图。全部检索排除 90-system，表格双链可点、任务复选框可写回源文件；库内零 JS 笔记，DataviewJS 保持关闭。
</views>

<explorer>
文件浏览器里每个文件夹右侧带一个计数（v0.16.0）。它回答的是这棵树自己知道却从不说出口的两件事：这里面攒了多少篇笔记，以及我到底有几个项目——ziminOS 的文件夹是容器（01-projects 装项目，一个项目装卡片，一本书装摘出来的划线），所以这两个问题是同一棵树的两个答案。常驻屏幕的只有一个数字（侧边栏以像素计宽，两个数并排会把文件夹名挤成省略号），口径由设置页「文件」页决定：笔记 / 文件夹 / 全部条目（含附件），另有一个「含子文件夹」开关，默认数笔记且含子文件夹。悬停在数字上把三项一起报出来，于是另一半答案不必回设置页换口径。空文件夹与库根一律不显示数字——一个 0 不解释任何事，而根那一行的标题被 Obsidian 与 Minimal 双双压平。计数不排除 90-system：那条「功能目录不参与检索」的纪律服务的是二十四个知识视图，而这里数的是「这个文件夹里有几个文件」，文件浏览器把它明摆在屏幕上，给一个减掉模板的数字就与用户点开看到的对不上。重算由 vault 的增删改名事件与一个 MutationObserver 驱动、防抖 80ms，无定时器无轮询；改名必须单独听事件，因为 Obsidian 复用同一个 DOM 节点只改 data-path，观察者看不见。关掉开关与卸载插件走同一段清理，DOM 里一个残留节点都不留。
v0.17.0 起这个模块又长出两件同源的事：**最近文件**（右侧栏一个 ItemView，记你打开过的最多五十篇、显示设置里那几条，可按打开时刻或文件修改时间排；**一行一篇、只有笔记名**，行高字号取 Obsidian 自己的 `--nav-item-*` 变量，于是与文件树里的一行长一个样——v0.17.0 由用户明令去掉了名字下面那行灰色路径：多一行不需要读的字，这张清单就从「同一棵树的另一种排法」变成了另一种控件；完整路径搬进悬停提示，两篇同名时问一句就有。它不随启动自动展开——日历是每天要看的坐标，这是找不回某一篇时才想起来的工具）与**状态栏当前路径**（点一下复制，命令「复制当前笔记路径」是它的第二个出口）。v0.21.0 给后者补了一个口径开关：**屏幕上永远是库内路径，复制走的那一串才随设置变**——两种路径不是「短的」和「长的」，是两种去处，库内路径说给 Obsidian 自己听（双链、跟同库的人说我那篇在哪儿），本机绝对路径说给库外面听（终端里 cd 过去、拖进别的程序、交给一个智能体去读那个文件），而猜错的代价不对称：猜短了粘进终端得到「文件不存在」，猜长了粘进笔记得到一条永远断的链接。显示不跟着走的理由同样具体——机器前缀对每一篇都一模一样，信息量为零而占宽最大，那一块只有 32ch 且带省略号，绝对路径塞进去被吃掉的恰恰是唯一有信息的笔记名。看与拿不一致因此当场说清：悬停提示写着这一下会复制什么。算绝对路径的那几行同版搬进 `core/localPath`，与外观的片段出境口共用一处——它走的是公开的 `FileSystemAdapter.getBasePath()`，**不构成新的红线缺口**。三件事合起来回答同一个问题：我在哪、有哪些、刚才去过哪儿——那就是「不会在自己的库里迷路」，因此同住「文件」那一页、只向设置页交回一个同步函数。
</explorer>

<editing>
打字时发生的四件事，它们同住「编辑」那一页——判据是同一个时刻：粘贴变成链接、附件交给 Eagle 与光标记住位置发生在你按下键的那一瞬间，排版发生在你离开这一篇之后。**粘贴成外链**：编辑器里选着字、剪贴板里是一条带协议的网址，Cmd+V 就写成 `[选中的字](网址)`；四条门槛（开关开着、这次粘贴没被别人处理过、真的选了字、真的是一条带协议的网址）全满足才动手。**Eagle 附件**（v0.22.0）：桌面端粘贴/拖入 File 时先接管默认行为，再由本机伴侣导入当前 Eagle 资源库，成功后只在 Markdown 写 `ziminos-eagle://v1/primary/{itemId}`；显示、打开都在 DOM/运行时解析，真实路径和端口不进笔记。同一资源库里移动项目或文件夹不改 `itemId`，所以链接不用更新；换库或搬动整库只需重新配对。导入失败 fail closed，明确报错且绝不偷偷在 Obsidian 本地留第二份。v0.22.1 把打开手势补到编辑器边界：阅读视图普通单击，实时预览/源码/YAML 属性值在 macOS 用 ⌘+单击、Windows 用 Ctrl+单击，单击仍留给编辑。v0.22.2 补足应用生命周期：最小化时定位后调官方 `app.show()` 恢复主窗口，完全退出时仅在用户点击之后以 `eagle://item/{itemId}` 让系统启动 Eagle 并直达附件，不存安装路径也不起后台轮询。v0.22.4 把项目/归档根的第一层容器归属接进导入事务；伴侣以官方 Folder API 创建或复用 `项目/容器名`，目录有重名歧义就停止。v0.22.5 增加默认关闭的图片分流开关：打开后纯图片事件在 `preventDefault` 前原样放行给已有图床，PDF、压缩包、音视频等仍进 Eagle；ziminOS 不复制图床配置也不保存密钥。v0.22.6 把归属规则收口为笔记路径的真相：项目、领域、资源、存档四棵根下的第一层容器都进 `项目/容器名`，不被更深的卡片目录改变；日/周/月/季/年日记不再伪装成项目，全部折叠到 Eagle 的单一 `日记`根目录。内容根下直接散落的笔记与其他路径不猜归属，继续读取非项目固定文件夹 ID。v0.22.9 收正点击的含义：伴侣不再用会切到“全部”的 `item.open()` 处理已归类附件，而是现查 item 的 folders，恢复窗口后打开它当前文件夹并选中附件；完全退出时先原生唤起，再于这次点击内做最长约 10 秒的有界重连，不改 Markdown，也不建后台轮询。**光标记忆**：离开一篇笔记时记下行列与滚动位置，下次打开回到那里，重启也还在；从带锚点的双链跳进来时不插手。位置存在 cursor-positions.json，不进 data.json。**排版**（modules/format）作为这一页的后半截交付。
v0.22.3 补齐分栏边界：光标记忆同时覆盖同分栏换文件与已打开分栏间换焦点，排版与 `updated` 的后台写入也会保住所有已显示分栏的视口。

**v0.33.0 把「在用户眼皮底下写盘」这件事收口了，判据被真机改正过一次。** 用户报「只要我输入内容，它就会跳转到图片的位置，页面在不停地跳」，并附了那个反复出现的弹窗——「已被外部修改，正在自动合并更改」。那句话是 Obsidian 说的，而它只在一种情况下出现：**磁盘上的文件变了，编辑器手里还捏着未保存的改动**。写它的是 `updated` 记账：`modify` 事件来自 Obsidian 自己的保存而不是用户停手，他保存后接着敲，两秒后那一笔就正好落在一个脏缓冲区上，于是合并，于是整篇重灌。这一篇的插图全是远端 URL，重灌等于把所有图片的高度清零再重量一遍——版面塌成一截、视口停在塌缩坐标上、图片再一张张加载回来把内容顶开，落定的位置由图片顺序决定，所以**每次都是同一张图**。那个位置不是任何代码选的，是塌缩再复原之后的余数。滚动保护救不回来，两条都不成立：合并晚于这次写入的 Promise，而捕获下来的滚动值指向的版面已经作废——**文件安全不会自动推出视图安全，而视图安全在文档被整份替换之后无从谈起**。旧设计明写着一句判断：「停手两秒」对 `updated` 那种只改一个 YAML 字段的动作足够，对整篇重排则远远不够；这句话现在被否掉了——**代价与写多少字节无关，只与写盘那一刻编辑器手里有没有未保存的改动有关**，字节数曾经看起来像判据，只因为它与真正的判据在排版那一侧恰好同时成立。修法既不是把两秒调长（调多长都是在赌用户什么时候停手），也不是加一层滚动补偿（那是事后补救一件本不该发生的事），而是让 `updated` 搬来排版早就立下的那条纪律：正开着的那一篇只记一笔「它脏了」，等他走开才补，落盘前再拿现场问一次；防抖只剩它该管的那一半——那些没开着的笔记。**「走开」包含关掉这篇笔记，而那一格是用户自己点出来的。** 他读完修复之后推出了同一个策略，并多说了一个触发点：「或者关掉这篇笔记的时候」。关掉最后一个标签页时事件照样触发，可 `getActiveFile()` 的官方语义是「当前视图不是 FileView 时返回**最近活动过的**文件」——也就是刚被关掉的那一篇，于是「我走开了」看上去从没发生，那笔账要等下一次开笔记才补、中途退出就丢了。**「开着」与「最近活动过」是两件事，洞就长在两者之差上。** 修法不是多订一个关闭事件，而是给「开在眼前的那一篇」下定义：活动文件**且**真的显示在某个 `MarkdownView` 分栏里——判据就是要防的那件事本身，没有分栏显示它就没有编辑器能捏着未保存的改动，于是「关掉」自动成为「走开」的一种。而这个定义**只能有一个**：补上钩子之后回归仍然红，因为落盘前那道闸还在问「是不是活动文件」，把钩子刚决定要写的那一笔又挡了回去——两处判断各自都对、合起来互相抵消、什么也没发生而且不报错，这类错比写错一个条件更难发现。反向那一格同时钉住：点日历、点文件树不算走开，因为那一篇仍显示在它的标签页里。这顺带把 `updated` 的语义摆正了：它说的是「最后一次被改是什么时候」，而**用户还在改，那个时刻就还没发生**。代价说清——连续编辑期间 `updated` 不再滚动更新，要等切走才落一次；换来的是用户能打字，而正开着的那一篇的 `updated` 本来就是全库最没人需要的一个值，他正看着它。

**v0.34.0 找到 `updated` 丢账的五处，而修法是承认「什么时候补写」这件事被写了两份。** 用户报「有的时候不知道怎么操作的，它的时间并不会更新，也不会写入」——「不知道怎么操作的」正是这类错的标准症状：每条丢账的路径单独看都是再普通不过的动作。按真机顺序喂事件逐条找出来：**改名**（在「未命名」里敲几个字、行内标题改名、写正文、切走——欠账按旧路径记着，走开时找不到就静默放弃；在项目目录新建卡片恰恰是这个顺序）；**退出**（欠账只活在内存里，写完直接关掉、手机被系统回收、重载插件，这一程都没了——v0.33.0 把它明写成「可以接受的代价」）；**记错时刻**（落笔取「此刻」，改完读一小时再切走就晚一小时）；**守卫误伤**（「所有写路径都 mark」让记人情、记付费、记收款、记灵感、勾待办、导入与同步划线、写主题七类写入全被跳过，而 §54.6 的验收明写着「记一笔人情，那篇日记的 updated 两秒内更新」——验收与代码在说相反的话）；**分栏**（左栏敲完最后几个字、两秒存盘没到就点进右栏，左栏已不在眼前却还捏着没存盘的字，写盘照样合并弹窗——v0.33.0 的判据是缓冲区干不干净，实现却问的是焦点）。找的过程中发现 formatter 那一份 `dirtyWhileOpen` 至今还在拿「最近活动过」当「开着」——**一件事写两份，修的时候就只修一份**，v0.33.0 已经演示过一次。于是这件事整件搬进 `core/editDebts`，两个模块只剩两个问题：哪一次变化算人改的，到点之后写什么。欠账跟着 rename 走；结算前让显示着它的分栏先 `save()`；写下的是**记账那一刻的磁盘修改时间**——**补记可以晚，记下的值不能跟着晚**，这一条也是退出能修的前提，否则补在下次启动就记成了第二天。欠账存进公开的 `App.saveLocalStorage`：不进 data.json（那是随库同步的设置），不落成状态文件（同步到另一台机器就是在替它补账，甚至补到它正在打字的那一篇上），不做启动时全库扫描（安装与同步会让几百篇集体改时间），也不在 `quit` 里当场补（那一刻 Obsidian 正在把各编辑器的字存盘，官方还明写不保证会跑）；下次启动时**磁盘修改时间对得上才补**，对不上说明关着的时候别处改过它，旧账作废而不是让 updated 倒退。排版的欠账不存——整理结果随时能从内容重算，**只有补不回来的事实才需要存**。守卫的语义同版写明：**mark 登记的是机器的反应，不是替人落下的笔**，全仓库每一处登记按文件归类钉在 `tests/writes.mjs`，新增一处不归类即红。那份回归造了一台跨重启的 Obsidian 替身，还记账「插件写盘那一刻哪几篇的编辑器捏着未保存的改动」——v0.33.0 那个判据第一次被直接断言而不是推断；九条里七条在修复前失败。仍在的边界写进规格 §59.8：正开着的那一篇在你看着时不动（纪律，不是遗漏）、3 秒守卫窗、改名引出的双链改写会被记账、两台同时开着的设备之间可能来回补记（v0.33.0 就在，本版未加重，未在真机双设备上复现）。
</editing>

<legacy>
Obsidian 1.6 把切换笔记库、帮助、设置三个按钮从左侧 ribbon 挪进了文件浏览器底下那一条，v0.17.0 把它们请回来（用户明令，见备案第 8 条）。做法不是搬 Obsidian 的 DOM、也不是复刻一条按钮栏，而是注册成三条 ziminOS 命令：于是命令面板搜得到、快捷键绑得上、「边栏」那页勾得掉、摆出来之后位置归用户拖。它们默认就摆出来——一个需要先去设置页勾选才回来的按钮等于没有回来；并单列成命令分组「旧版」着中性灰，因为它们做的是 Obsidian 自己的事，不属于 ziminOS 的任何一摊。上游那个插件（Quorafind/Obsidian-Legacy-Vault-Switcher）**没有许可证**，所以连打包交付这个选项都不存在。
</legacy>

<config>
docs/插件代码审计修复报告-2026-09-12.md - 本次代码审计的缺陷、触发条件、回归证据与真机验证边界；实现契约见设计规格书 V2 §61，各模块 L2/L3 同步
AGENTS.md - 智能体任务路由，**桌面智能体自动读到的第一份指令**，因此是全仓库路由的第一现场。它按版次分流：提到三库/pro/付费或点名另外两本库 → skill-pro/SKILL.md，否则 → skill/SKILL.md；开发请求进入项目规格。v0.17.0 重写：它从第二版落库（b630b4a）起一直停在「只有一本库」的世界，写着无条件的「安装请求→skill/SKILL.md」与「不得创建另一层目录」——后一句恰好把三库布局明令禁止了，于是拿着第二版指令来的智能体被它劫持成第一版，只装出一本库且**不报错**。教训是它必须与契约同增同减，因此带 [PROTOCOL] 头并由回归钉住：每一份存在的施工契约都必须出现在它的路由表里
README.md - GitHub / Gitee 共用的公开首页与安装入口。默认可复制的两段安装、升级口令指向 GitHub：第一版 `zhaozimin/ziminOS`、第二版 `zhaozimin/ziminOS-Pro`；折叠区保留 Gitee `ziminzhao/zimin-os-v1` / `ziminzhao/ziminos-pro` 国内镜像。首页不复制施工逻辑，只把两个版次导向各自契约；免 Git 安装包目前仍明确指向 Gitee `releases/latest`，不把源码镜像写成已经存在的双 Release。v0.21.0 的手机口令解决远程窗口读不到系统根认路文件的问题，并以 `notectl.py status` 自证连接；v0.22.0 的 Eagle 入口则只把学员导向单文件 HTML 手册，不在首页再造一份安装契约
package.json - 依赖与六条脚本：dev 常驻 watch，test 跑插件审计回归，build 先 tsc 严格检查、esbuild 打 Obsidian 主包再调用 package-eagle.sh，package:eagle 可单独重建伴侣包，demo 由 docs/export-demo/ 的四份源重新生成那份交互演示页，check 串行构建与测试
package-eagle.sh - Eagle 伴侣唯一打包出口；核对两端版本后以固定白名单、权限与时间戳组装可复现的 `.eagleplugin` ZIP，产物原子落进 vault 的 ziminOS 插件目录，不安装、不读取 Eagle 用户数据
publish-v1.sh - Pro 事实源到第一版 GitHub / Gitee 两个公开镜像之间**唯一**的同步通道，方向只有 pro → v1 一条。它把两版共享的 src / vault / eagle-companion / fonts / skill / tests 与构建配置单向发布；README / AGENTS / CLAUDE / docs 不在同步范围，两个版次各自说给各自的读者听。方向不能反：pro 的 src/ 是 v1 的**严格超集**，从 v1 往回同步会删掉第二版。它存在的理由是一次事故——两个仓库曾各自能改同一份 src/，分叉出 editing/explorer/legacy 与 eternal/edition 两批改动，最后靠一次 23 个文件的三方合并才收回来。发布前先对 GitHub / Gitee 两个目标做非强制 dry-run，任一边分叉或无权限就不写；随后推的是同一提交。其余四道闸不变：工作区必须干净、`npm run check` 必须过、构建后 main.js 不许再变、同步后的目标仓库必须自己跑得过回归。防泄漏是硬断言：目标里出现 skill-pro / vault-pro / edition.json 一律中止
make-pro-package.sh - 第二版的打包出口，也是第二版 Gitee 发行版上那个附件的来源：没有 Git 的机器上，智能体就靠它取施工源。工作区干净、`npm run check` 通过且构建后无改动才动手，交付物只从 HEAD 取（`git archive`）并断言私有状态不出门——它现在是谁都能下载的公开物；包名 `ziminOS-pro-v{版本}.zip` 只用 ASCII、不带日期，时间戳取提交时刻，同一个提交重打得到同一个 zip。第二版源码的两个同步镜像是 Gitee `ziminzhao/ziminos-pro` 与 GitHub `zhaozimin/ziminOS-Pro`；分发包暂时只挂 Gitee，clone 则按用户入口选择任一同版镜像。GitHub `zhaozimin/ziminOS` 属于第一版，不能因主机相同就混用
upload-release.py - 发行版的唯一上传出口：经 Gitee OpenAPI 建（或复用）发行版、上传附件，再从学员走的公开下载通道逐个核对字节数。令牌取自 macOS 钥匙串（service `ziminos-gitee-token`，用户在终端存一次），不进命令行参数、不进输出、不进文件；附件名必须是 ASCII。它存在是因为发版最后一步原本要人手拖文件，而发行版上没有包，没有 Git 的机器就只剩克隆那条慢路
make-v1-package.sh - 第一版面向「人」的分发出口：不用智能体、不用装 Git，下载解压照《安装说明》自己装。三条判据：清单不另写（「装完该有什么」「升级换什么」从 skill/SKILL.md 解析，解析不出 main.js 或升级分支开始替换 data.json 就中止）；包是第一版仓库 main 的另一种形态（`git archive HEAD` 取料，被 .gitignore 挡住的开发库状态带不出门，再经推送用的 SSH 通道只取第一版仓库 main 的提交与树（`--filter=blob:none`，不走对未登录请求限流的开放接口）比对 vault/ 与 fonts/ 的树哈希，对不上就先 publish-v1.sh；不重复构建，因为 esbuild 会把 node_modules 的相对位置写进注释）；布局给人看（ziminOS 笔记库 / 只放字体的「字体」/ 只放程序、按 Obsidian 三枚「打开…文件夹」按钮分组的「升级文件」/ 许可证 / 安装说明.html）。产物 `ziminOS-v{版本}-setup.zip` 挂第一版仓库的 Gitee 发行版：匿名可下，单附件 ≤100MB、全仓附件 ≤1GB，旧版附件要定期删；它同时是 skill/SKILL.md 取法一的施工源，包名只用 ASCII 就是为了让智能体用 Python 拿得下来
pack-zip.py - 两个分发包共用的唯一 zip 写出口；中文名一律置 UTF-8 标志位（macOS 自带 zip 不置，Windows 自带解压会解出乱码目录名），条目顺序与时间戳确定、写完逐条核 CRC。从 make-pro-package.sh 抽出来是因为第二个包出现了——同一段知识写两份，迟早只修一份，修漏的那份不在打包时报错
tsconfig.json - 严格模式 + noEmit；类型检查与代码产出彻底分工，产出只由 esbuild 负责
用户先创建并命名文件夹 A，再用桌面 Agent 打开 A；GitHub / Gitee 共用 README 顶部那段「一分钟安装」是对人的入口，默认分支的 skill/SKILL.md 是 Agent 的唯一施工契约。此时 A 同时是 Agent 工作区与最终 Obsidian 笔记库。安装时不再询问名称或路径，不创建子目录，不在 A 内克隆源码；官方源码镜像只能进入 A 外部的系统临时目录，最终把 vault/ 的内部内容直接铺到 A 根。全新安装交付锁定的 Dataview、Outliner、Quiet Outline、外观包（含十三个 CSS 片段与它们的默认启用清单）、`.obsidian/.gitignore` 隐私护栏与 fonts/ 的四款字体——字体装进用户级字体目录（免管理员，不碰系统级），appearance.json 的 textFontFamily 预设文楷 GB 屏幕版，正文换字走 Obsidian 官方设置正门、插件零参与；升级只更新受管运行文件、合并必要开关与缺失的隐私忽略规则，绝不覆盖用户配色、用户自己放进 snippets/ 的片段、其他插件配置（含 Outliner 与 Quiet Outline 各自的 data.json）、ziminOS 自己那三份运行时状态文件（holiday-cache / recent-files / cursor-positions）、用户自选的正文字体、用户已有忽略规则与用户字体目录里已存在的同名文件。任何安装或审计流程都不得输出可能含微信读书 Cookie 的 ziminOS data.json；禁止让用户打开仓库或仓库内的 vault/，禁止安装 Node/npm 依赖，交付后必须清理临时源码。
docs/插件代码审计修复报告-2026-08-18.html - 对外交付的单文件审计报告；源码与设计规格仍是事实源
</config>

<delivery>
esbuild.config.mjs - Obsidian 构建出口；打包前把 package.json 版本同步到 Obsidian 与 Eagle 两份 manifest，主产物直接写入 vault 插件目录，再由 package-eagle.sh 生成伴侣包。产物里的依赖路径一律归一成 `node_modules/` 开头：esbuild 写的是相对工作目录的路径，在 git worktree 里构建会得到 `../../../node_modules/`，代码一字不差、main.js 却差出几百行，publish-v1.sh 的「构建后 main.js 不许再变」就随构建地点时红时绿
.gitignore - 忽略依赖、系统杂项、历史发布压缩包、常见凭据、开发库私有状态与发布/Eagle 打包暂存；main.js、`.eagleplugin` 与公共 vault 资产不忽略，学员浅克隆即可用
.gitattributes - 锁定 Dataview、Minimal、Style Settings、fonts/ 字体及 Eagle 图标/安装包等二进制发布资产的原始字节，防止 Git 换行/格式化破坏 SHA-256
docs/第三方组件.md - 运行依赖、独立交付资产与外部设计参照的版本/上游/许可真源；v0.23.0 增加 dom-to-image-more、jsPDF 及两款导出参考插件边界
docs/设计规格书-V2.md - v0.4.0 起的唯一设计事实源；§26–§42 记录既有演进，§43–§63 追加微信快捷收集、导出、客户、安装发布、写入安全与重名纠错，§64 定义 GitHub / Gitee 双镜像及「源码双镜像、发行包仍在 Gitee」的边界。与 V1 规格并存，交集处以 V2 为准
docs/导出预览交互演示.html - 导出弹窗的单文件交互演示，**生成物**（`npm run demo`，真源在 docs/export-demo/）。它存在的理由是一条判据：**示意图会撒谎，生成物不会**——手抄的仿真页在代码改过之后仍然一动不动地讲旧故事，而且没人会在那一刻发现。因此页内的导出样式逐字取自插件 styles.css、几何与装饰由 esbuild 从 src/ 直接打包、控件清单运行时读 EXPORT_SLIDERS，改了插件却忘了重新生成会被回归当场拦下。可离线打开、可转发，读者能直接拿自己的 logo 试
docs/第一版手动安装指南.html - 由 make-v1-package.sh 原样复制成包里的「安装说明.html」；全新安装五步、验收、手动升级与常见问题，覆盖 Windows/macOS，零脚本（带网络标记解压出来的文件脚本不一定跑得起来）。它翻译契约不另造做法：升级只换契约点名的程序文件，唯一铁律是「只拖文件，不拖文件夹」——macOS 访达把文件夹拖到同名文件夹上选「替换」会连同 data.json 整个换掉
docs/Eagle附件桥接安装与使用指南.html - 面向学员的单文件安装说明；把三库升级口令、Eagle 伴侣安装/配对、无本地副本验收、四类内容容器/日记分流、移动附件语义与端口/换库/图床冲突排障收成一份 macOS/Windows 可转发手册
</delivery>

<deviations>
十二处偏离，在此备案，不是疏漏：
1. tsconfig 的 moduleResolution 取 bundler 而非规格书 §2 写的 node —— 实际装到的 TypeScript 7.0.2 已移除 node10 解析模式（TS5108），规格的 node 与规格的「依赖用最新稳定版」自相冲突；bundler 是 esbuild 打包场景下的等价现代取值，其余编译选项全按规格保留。
2. src/core/time.ts 存在全仓库唯一一处类型断言 —— obsidian 把 moment 作为命名空间导出，其类型不携带调用签名，断言只还原类型不改变运行时行为。
4. 读书笔记模块联网 —— 见开头那段：用户明令放宽第二条红线。落地上只有两个出口：`modules/books/douban.ts`（豆瓣搜索页与详情页，走 Obsidian **公开** API requestUrl）与 `modules/books/sourceWeread.ts`（微信读书：书架走 Cookie 的 /api/user/notebook，划线与想法走取数网关 i.weread.qq.com/api/agent/gateway，令牌在 /api/skills/apikeyGet 用登录态换，全部经 requestUrl）。两处都不碰账号密码；豆瓣不需要登录，微读的登录态由用户扫码后从会话里取。删掉 modules/books 即让零网络重新成立。
5. src/modules/books/sourceWeread.ts 运行时 `require('@electron/remote')` 取 BrowserWindow —— 「只用官方公开 API」这条红线的第二处缺口，为的是让微信读书的登录是**扫码**而不是让学员去开发者工具里手抄一长串 Cookie。它按与第 3 条同样的三条纪律收窄：只有开登录窗口这一步借用（取数全走公开的 requestUrl）、探不到就整条命令降级为不可用而绝不抛异常、声明与调用同处一个文件。另注：那段登录轮询有明确起止与用户在场，与「无后台轮询」说的不是一回事。
6. src/modules/calendar/holidays.ts 联网——日历只在视图打开或用户切年时，经 Obsidian 公开 `requestUrl` 按顺序访问 holiday-cn 的 jsDelivr / Fastly / GitHub Raw 镜像，校验年份、日期、重复与国务院公告链接后才写入 `holiday-cache.json`。这是 v0.15.0 用户明令要求「调休无感更新」的第三个网络出口；无定时器、无轮询、无凭据，失败时保留最后一份好数据，删掉 modules/calendar 即让本出口消失。
7. src/modules/explorer/badge.ts 依赖文件浏览器渲染出来的 DOM——`file-explorer` 这个视图类型名、`.nav-folder-title` 这个类名、`data-path` 这个属性。它**不是**第三条那样的非公开 API 缺口：找容器走的是公开的 `getLeavesOfType` 与 `View.containerEl`，社区里那些「文件数」插件读的 `view.fileItems` 一个字都没碰；依赖的是主题与 CSS 片段共用的同一层约定（Minimal 自己就有七条规则指着 `.nav-folder-title`）。仍然备案，是因为它毕竟是宿主的渲染形态而非它承诺的接口。按与第 3 条同样的三条纪律收窄：四条约定与调用处同处一个文件、探不到叶子或找不到那个类名时整条功能静默缺席（不抛异常、不提示、不降级成别的样子）、删掉 modules/explorer 即让本条消失。
8. src/modules/legacy/vaultDock.ts 借三处 Obsidian 非公开成员——`app.setting.open`、`app.openVaultChooser`、`app.openHelp`。这是「只用官方公开 API」这条红线的**第二处**缺口，v0.17.0 由用户明确授权后开的。`obsidian.d.ts`（1.13.1）里 App 的公开成员只有十项（keymap / scope / workspace / vault / metadataCache / fileManager / lastEvent / renderContext / secretStorage / isDarkMode），开设置、开库选择器、开帮助一件都不在其中；按 id 执行命令的 executeCommandById 同样不在，所以那条路也不是出路。判据与第 3 条同源：不碰，「把那三个按钮拿回来」就不成为一个功能。上游那个插件没有许可证，因此打包交付这个选项也不存在。三条纪律逐条对齐第 3 条：只借「点下去要发生什么」这一步（摆按钮走公开的 addRibbonIcon 与命令注册台）、模块增强声明成可选并在**点下去的那一刻**验形、探不到只弹一句人话（照 CommandRegistry 的纪律，命令不因探不到而消失——一个会隐身的图标比一句提示更让人困惑）、声明与调用同处一个文件，删掉 modules/legacy 即让本条消失。
3. src/modules/appearance/snippets.ts 借了一次 Obsidian 非公开 API，是这条红线的**第一处**缺口（v0.17.0 之前是唯一一处，另一处见第 8 条）—— 「让某个 CSS 片段此刻生效或失效」在 obsidian.d.ts（1.13.1，8482 行）里没有入口，全文既搜不到 customCss 也搜不到 snippet；能做到的只有 app.customCss.setCssEnabledStatus。不碰它，开关就退化成「改配置文件 + 请重启」，也就不再是开关。缺口按三条纪律收窄：其一，只有「让改动生效」这一步借用，片段清单与启用状态全部走公开的 vault.configDir + DataAdapter，因此开关显示的永远是磁盘上的事实；其二，用模块增强声明成可选成员并在运行时二次验形，TypeScript 强制判空，探不到就降级为改 appearance.json 并提示重载，功能退化但绝不抛异常；其三，声明与调用同处一个文件，不散进 .d.ts，删掉 modules/appearance 即可让红线重新完整。
9. 第二版的跨库拷贝**没有**成为第四处红线缺口，这条记下来是因为它差一点就是。归档时要把项目副本送进《赛博永生》，而 Obsidian 的 vault API 被沙箱锁在本库内；借 Node `fs` 写隔壁库能做到，代价是「只用官方公开 API」第三次破例，且手机上归档必炸。选择的是 `src/modules/eternal/`：插件只往本库一篇可见的 Markdown 上追加一行出库单，真正的拷贝与提炼由桌面智能体完成。判据不是保守——**提炼本来就只有 LLM 干得了**，让它一次进门把搬运与提炼一起做完，比让文件先躺过去等着更顺。代价是拷贝不是「点完立刻出现」而是「下次和智能体说话时出现」，这一点必须在对用户的说明里说实话，不许含糊成「自动同步」。
10. src/modules/appearance/reveal.ts 运行时 `require('electron')` 取 shell —— 「只用官方公开 API」这条红线的第四处缺口，也是**第二次直接向 Electron 伸手**（第一次是第 5 条那个扫码窗口）。为的是让外观开关从「只能开关」变成「能改」：面板列得出十三个片段，此前却没有任何办法把其中一个交到用户的编辑器手上，而从「看见它」到「改到它」中间隔着退出面板、找到笔记库文件夹、打开系统默认隐藏的 `.obsidian`、进 `snippets`、在十三个中文文件名里认出刚才那一个。`obsidian.d.ts`（1.13.1，8482 行）全文搜不到 shell、openPath 与 showInFolder，App 的十个公开成员也没有一个沾边，不碰就不成为功能——判据与第 3、8 两条同源。**借的只有最后一步**：绝对路径走公开的 `FileSystemAdapter.getBasePath()`（标着 @public 的导出类），因此「文件在哪」全程由 Obsidian 回答，Electron 哪天没了，坏掉的是两个按钮而不是路径、清单与开关。三条纪律逐条对齐第 5 条：只借「打开」这一个动作（清单、状态、路径全部走公开 API）、先验 Platform.isDesktopApp 再 require 且全程 try/catch 返回可空（手机端与换掉运行时都只让按钮消失或说一句人话，绝不让插件加载失败）、声明与调用同处一个文件，删掉 modules/appearance 即让本条消失。「画不画」与「点了会怎样」刻意分开答：前者只认公开且稳定的事实（桌面端 + 本机文件系统），手机上静默缺席；后者在点下去的那一刻才验形，桌面上按钮照画，探不到时给一句为什么而不是让按钮凭空少掉。
11. src/modules/eagle 的桌面附件边界按需使用 Electron `webUtils.getPathForFile`、`shell.showItemInFolder`、`shell.openExternal` 与 Node 临时文件 API —— 前两者分别用于取得拖入 File 原路径与显示随包交付的 `.eagleplugin`；`openExternal` 只在用户点击链接且回环伴侣根本不在线时，把经严格验形的 `eagle://item/{itemId}` 交给操作系统唤起 Eagle，随后仅在这次点击里做最长约 10 秒的有界回环重连，不存安装路径、不启动后台轮询，也不处理 HTTP 发来的业务报错。截图等无原路径的内存 File 才短暂落入系统临时目录，导入后立即清理。这些缺口都在统一平台闸门确认 macOS/Windows Obsidian 桌面端之后动态 require、全程可失败，其他平台加载阶段不解析 Node/Electron。真正的资源写入不靠这些缺口：Obsidian 端走公开 `requestUrl`，Eagle 端只调用官方 `eagle.item.addFromPath/getById/open/select`、`eagle.folder.getAll/create/createSubfolder/open` 与 `eagle.app.show`；删掉 modules/eagle 与 eagle-companion 即让本条及本机回环/唤起出口一起消失。
12. src/modules/export 在桌面端动态探测 Electron 的两个系统对话框与对应的 Node 文件调用：`exporter.ts` 用 `dialog.showSaveDialog` + `fs.writeFile` 让用户把导出文件存到库外任意位置；`logoImport.ts`（v0.25.0）用 `dialog.showOpenDialog` + `fs.readFile` 让用户直接从电脑上挑品牌标志，读进来之后立刻经公开的 `vault.createBinary` 落进 `90-system/导出标志`——**本机绝对路径一个字都不进设置**，存的仍是库内路径。两处同一姿态：探不到即降级（保存回落 Vault API，选图回落「从库里选」），移动端加载阶段不解析 Node/Electron，声明与调用同处一个文件。Obsidian 公开 API 没有系统级“另存为”；探不到时回落到官方 Vault API 保存到当前笔记旁边，取消则零写入，移动端加载阶段不解析 Node/Electron。Markdown 渲染、资源读取与库内回落全部走公开 API；删掉 modules/export 即让本条与 dom-to-image-more/jsPDF 两个运行依赖一起消失。
</deviations>
