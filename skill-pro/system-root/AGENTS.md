# ziminOS 三库系统 · 系统根

**这个文件夹不是笔记库**，是三本笔记库并排住着的地方。动手前先读同目录的 `CLAUDE.md`，那是完整版。

三本库：`兼收并蓄`（进料口）、`以人为本`（工作台）、`赛博永生`（成品库）。**真实目录名以磁盘为准**——扫描本目录的直接子目录，读第一份 `.obsidian/plugins/ziminos/edition.json`，它的 `vaults` 是唯一事实源。

日常干活读本地契约，不要联网取：

- 「记一下……」→ `.ziminos/skills/capture/SKILL.md`
- 「提炼一下」/ 粘来一条项目 MOC 路径 / 提问 / 体检 → `.ziminos/skills/distill/SKILL.md`
- 「更新 ziminOS」→ `https://gitee.com/ziminzhao/ziminos-pro/blob/main/skill-pro/SKILL.md`

三条红线：**用户原话一个字不许改**（清洗与排版交给 `.ziminos/skills/scripts/notectl.py`，你只判断归类）；**《赛博永生》的 `10-原料/` 只读**；**不要在系统根里创建笔记**。

不确定就问，不要猜——每一次写入都落在他的真实笔记上。

[PROTOCOL]: 与同目录 `CLAUDE.md` 同增同减；它由 skill-pro/SKILL.md 在安装时铺开。
