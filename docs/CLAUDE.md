# docs/

> L2 | 父级: ../CLAUDE.md

## 成员清单

设计规格书.md: V1（v0.3.1，项目管理与灵感收集）的唯一设计事实源；描述已交付形态，是 V2 之前一切行为的判据。
设计规格书-V2.md: V2（v0.4.0 起）的追加式设计事实源；§26–§36 记录日历、部署、三库与 Eagle，§37–§42 记录分栏保护、Bases 迁移、废弃语义与客户 MOC；§43 定义 v0.23.0 微信快捷收集只进灵感集、当前笔记整张 PNG/单页 PDF 导出、客户答疑回链，以及 `MOC-人脉` / `MOC-客户` 的初始化命名与迁移隔离；§44 定义 v0.23.1 复盘笔记的自动认领——诞生事件而非命令入口决定套不套模板，含五道闸、双事件监听与归位边界；§45 定义 v0.24.0 的导出预览——内容与装饰按寿命分层、预览即导出物本身、水印几何与九宫格落点、品牌标志的一路径三尺寸，以及风格落盘的时机；§46 定义 v0.25.0 把版面口径交还给编辑区（宽度/留白/字号全部量真的，不再发明）、三处文字颜色的「跟随」默认态、不移动文字的列表参考线、从电脑直接选标志，以及只在 PDF 成立的页眉页脚链接；§47 记录 v0.26.0 从一份真机 PDF 上查出的五处错——参考线画给顶层、编辑态量错元素、纸停在视口外让 Bases 只剩空表格、正文外链导出后变死字、以及「先问去处再做图」的顺序纠正；§48 定义 v0.27.0 的纸张两边自适应/自定、为什么高度只能是下限、以及读数变成可输入数字框；§49 记录 v0.28.0 把左右页边取平均摆正正文（量的是版面，不是屏幕留给滚动条的补偿件），以及用按阶段自报家门的进度弹窗取代那条「像盲盒一样」的角落提示；§50 定义 v0.29.0 的链接修复（只填链接也成行、下划线只在 PDF 下画）与导出明暗（挂舞台的类，不动界面主题）；§51 定义 v0.30.0 的紧凑控件列（说明不常驻、关着的段落不画、三个开关的升级语义）与日历的两种着色（今天与写过了是两个事实，不是三选一）；§52–§54 记录 v0.31.0 到 v0.33.0 的三轮真机修复（宿主同名方法遮蔽与导出明暗、PDF 可点区域落在纸外、在用户眼皮底下写盘）；§55 定义第一版不经智能体的手动安装包——同一份交付物换一种形态、清单从契约解析、只从 HEAD 取料并与第一版仓库对账、升级只换程序不做合并，以及选 Gitee 发行版的实测依据；§56 记录一份 Windows 新电脑上的二十分钟真机复盘——六类根因各自怎么消掉、第二版验收清单自相矛盾的两处、为什么两份契约的施工源都改成先取发行版附件、第二版分发包成为公开下载物之后补上的三道闸，以及顺带修掉的发布闸误报；§57 定义全新安装从契约散文变成代码——为什么是「一个 Python 核心 + 两个启动脚本」、Python 从哪来且怎么钉校验值、PowerShell 5.1 的四个坑、结果文件与失败回滚、升级为什么暂时留在契约里，以及上传从人手拖文件变成钥匙串令牌加脚本；§58 记录 v0.33.1 代码块行号片段的折行修复——`text-indent` 只缩进首行、原意的内边距输给了原生规则，以及为什么一处 CSS 也必须升版本号发版；§59 记录 v0.34.0 找到的 updated 丢账五处（改名、退出、记成落笔的时刻、自写守卫误伤替人落笔的命令写入、分栏里捏着没存盘的字）——为什么「什么时候补写」要从两个模块搬进 core/editDebts（一件事写两份，修的时候只修了一份）、为什么写下的是改完那一刻的磁盘修改时间、欠账为什么存进本机 localStorage 而不是 data.json / 状态文件 / 全库扫描 / 退出时当场补、守卫登记的语义与归类清单、分栏的判据是缓冲区而不是焦点，以及仍然存在的四条边界与验收；§60 定义 v0.34.1 的书籍双重身份与导航封面看板——真实书名进入 aliases，MOC 文件名继续承担结构身份，卡片标题显示别名且保持可点击，初始化与三种历史导航的显式迁移共用同一终态。
第三方组件.md: 全仓库供应链锁定与外部设计参照表；新增 dom-to-image-more 3.10.2 与 jsPDF 4.2.1 的 MIT 运行依赖，并记录 Export Image / Better Export PDF 只作设计参考、不复制源码；其余算法、插件、图标、字体、片段、Eagle 官方 API 与平台边界继续在此锁定。
第一版手动安装指南.html: 第一版安装包里那份「安装说明.html」的真源，由 make-v1-package.sh 原样复制进包。它回答的是「没有智能体、没有 Git 的人怎么装」：全新安装五步（解压 → 装字体 → 放好并改名 → 打开并信任 → 初始化）、五处验收、手动升级与常见问题。按钮名取自 Obsidian 中文界面与插件真文案，升级落点借 Obsidian 设置里的三枚「打开…文件夹」按钮，于是学员不必知道 `.obsidian` 在哪儿。零脚本；包内名字、契约的程序文件清单与插件文案由 tests/packaging.mjs 钉住同构。
Eagle附件桥接安装与使用指南.html: 面向学员的单文件安装手册，按“升级 ziminOS → 覆盖安装 Eagle 伴侣 → 配对 → 四类内容容器/日记分流与验收 → 跳转手势/当前文件夹定位/退出唤起/移动语义与排障”展开，覆盖 macOS/Windows 且不引导学员接触源码、令牌或 data.json。
导出预览交互演示.html: **生成物**，真源在 export-demo/，不要手改。单文件、可离线打开、可转发：让人不装插件就能看见导出弹窗长什么样、拖动时怎么反应，并能直接拿自己的 logo 试。页内的导出样式逐字来自插件 styles.css，几何与装饰由 esbuild 从 src/ 打包，控件清单运行时读 EXPORT_SLIDERS——三处事实都来自真源，因此它不可能与产品说两样话。
export-demo/: 上面那份 HTML 的四份真源（entry.ts 接缝 / shell.html 外壳 / driver.js 驱动 / build.mjs 生成器），由 npm run demo 调用，不进插件构建链。
插件代码审计修复报告-2026-08-18.html: 本轮 13 项缺陷的独立可打开交付摄影；记录 v0.14/v0.16 修复对照、回归与构建证据、新分发包 SHA-256、仍需真机验收的边界与旧包保留策略。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
