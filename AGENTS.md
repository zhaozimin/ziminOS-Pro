# Agent 路由

本仓库交付**两个版次**，各有一份互不引用的施工契约。**先分清用户要装哪一版，再动手。**

装错版次不会报错，只会让用户得到一套不是他要的东西。判断不了就问，不要猜。

## 一、先判版次

| 用户怎么说 | 执行哪一份 |
| --- | --- |
| 提到**三库**、**第二版**、**pro**、**付费版**，或点名《兼收并蓄》《赛博永生》 | `skill-pro/SKILL.md` |
| 只说「搭建 / 安装 Obsidian 笔记库」「安装 ziminOS」，以上词一个都没提 | `skill/SKILL.md` |

用户给的链接指向哪一份，就以哪一份为准——链接比词句更明确。

## 二、第一版 · 一本笔记库（`skill/SKILL.md`）

当前工作区**就是**最终 Obsidian 笔记库。不得询问名称或路径，不得创建另一层目录，只把仓库 `vault/` 的内部内容铺到当前工作区根目录。

## 三、第二版 · 三本笔记库（`skill-pro/SKILL.md`）

当前工作区是**系统根，不是笔记库**。必须在它里面建三个并排的文件夹，每一个都是一本独立的 Obsidian 库：

```text
用户打开的文件夹/     ← 系统根，它自己不是笔记库
├── 兼收并蓄/         ← 笔记库 1：进料口
├── 以人为本/         ← 笔记库 2：工作台（= 第一版那本库）
├── 赛博永生/         ← 笔记库 3：知识成品库
└── .ziminos/         ← 智能体自己的契约副本
```

**上一节那句「不得创建另一层目录」在这里不成立**——它是第一版的约束，不是全局规则。第二版必须创建这三层，否则就是装错了。

## 四、装完自检

第二版装完，系统根下面必须有且只有 `兼收并蓄/`、`以人为本/`、`赛博永生/` 与隐藏的 `.ziminos/`。

**出现下面任何一种，说明执行的是第一版契约，立即停下来告诉用户，不要继续：**

- 只长出一本库；
- 工作区根目录直接冒出 `00-inbox/`、`01-projects/`、`90-system/` 这类 PARA 目录（那是第一版把笔记库原地铺开的样子）。

## 五、两版共同

- 施工源只能临时放在工作区之外，不得在工作区内下载、解压或克隆。
- **施工源先取 Gitee 发行版上的包，走不通再 `git clone`**：版本号从 raw 读 `manifest.json`、包从 `releases/download` 取，不需要 Git、不需要登录，一台没装过开发工具的 Windows 电脑也取得到。**不要用 `api/v5` 开放接口找发行版**：它对未登录请求限流（403 Rate Limit Exceeded）。不要下载仓库的「下载 ZIP」（未登录只拿到 HTML 跳转页），不要用 raw 地址逐个文件拼仓库（大文件 403、个别文件 451）。两份契约的第二节各有 Python 与 PowerShell 写法。
- 不得安装 Node.js、npm 依赖或任何额外插件、主题、字体。
- 施工源仓库**按版次分开**，取错了不会在下载或克隆那一步报错：
  - 第一版 → `https://gitee.com/ziminzhao/zimin-os-v1.git`
  - 第二版 → `https://gitee.com/ziminzhao/ziminos-pro.git`
  第二版取了 v1 仓库，clone 照样成功，缺 `vault-pro/` 要等到交付物清单才发作；第一版取了 pro 仓库更糟——它会把整份施工源克隆到用户机器，等于让每个免费用户顺手把付费交付物拉到本地。
- GitHub 上的 `zhaozimin/ziminOS` 已经访问不到（403），不要用它，也不要去找镜像。

## 六、开发任务

如果任务是开发或维护 ziminOS 本身（而不是给用户安装），读 `CLAUDE.md` 与 `docs/设计规格书-V2.md`。

[PROTOCOL]: 新增版次或改动任一契约的入口时，必须同步本文件——它是桌面智能体读到的第一份指令，漏改的表现是安装静默走错版次而不报错。
